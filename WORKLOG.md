# Worklog — autonomous runs

Append one dated entry per run (newest at the bottom). Keep entries short: what changed, tests, commits, next.

## 2026-09-06 00:30 (interactive session)
- v0.3 pushed to GitHub (ugurozsahin/sky-academy, main). Hourly routine to be created on claude.ai/code/routines with this repo attached.

## 2026-09-06 03:40 (hourly routine, cloud)
- Backlog #5 done: first-play tutorial — `#tutorial` element (CSS hand + demo bubble pop + "Slice the bubble!"), spoken cue, first wave held 1.8 s, hides on first hit (or after 9 s); `tutorialSeen` flag in save data (old saves migrate to `false`).
- Backlog #6 done: `voiceScore`/`chooseVoice` in `audio.ts` rank en-GB child/warm/natural voices first (Maisie > Libby/Sonia/Google UK Female > …), Apple novelty voices excluded; voices-not-loaded-yet no longer pins `null`. Tap anywhere on the question card = repeat aloud (pulse animation).
- Bug found & fixed in `arena.ts`: slice hit-test used the fading visual trail, so a finger that paused >280 ms lost its next segment. Now tracks `lastPt` separately.
- e2e: "real swipe" test was flaky on baseline (3/4 fails here) — pointer round trips take 400–800 ms under headless software rendering so bubbles drifted through/away from the stroke. Test now freezes the wave via the arena hook and approaches from a clear side; 8/8 passes.
- Skipped (owner input): #1 playtest, #2/#3 owner art, #4 bubble skins (visual style decision).
- Tests: unit 191/191, tsc clean, e2e mobile 14/14, e2e desktop 14/14. Commit 2a84fda (main).
- Next: Proposed P1 (Ninja Sprint) or Next #9 (Y1/Y2 shape/measure/statistics topics) — both need no owner input.

## 2026-09-06 04:40 (hourly routine, cloud)
- Backlog P1 done: **Ninja Sprint** — `mode: 'sprint'` in `session.ts` (60 s clock via `tick(ms)`/`onTime`, no lives, 10 pts + combo bonus, difficulty ramps 1→2→3 at Q5/Q12, steady stage-2 speed; stars 1/6/12 correct, coins = correct + 5×stars). `storage.sprint` per-year best + `recordSprint()` (old saves migrate). Island screen: "Ninja Sprint" button under Sky Storm showing best. Play HUD: timer pill replaces hearts, turns red + pulses at ≤10 s, ticks at 3-2-1; clock freezes while paused. Results: "Time's up!", stars, "🏆 New best!" pill.
- Tests: unit 196/196 (+4 sprint session, +1 storage), tsc clean, e2e mobile 15/15 (+1 sprint test: no lives lost, clock pauses, fast-forward → results → best saved).
- Skipped (owner input): #1 playtest, #2/#3 owner art, #4 bubble skins.
- Backlog P2 done: **Boss Battle** — `mode: 'boss'` (`BOSS_HP = 8`): correct slice → `bossHp−1` + `onBoss('hit')` (villain shake, white flash), wrong/miss → heal +1 capped at max + taunt; lives work as in missions (Reception gentle); `enraged` at ≤3 HP → stage-3 speed; TNT bubbles like Sky Storm; KO ends won, stars by accuracy (90/70 %), coins = correct + 5×stars + 20. `storage.boss` counts KOs per year (`recordBossWin`). Island button "Boss Battle · KOs n"; HUD health bar over Hammer Man (turns amber when low); results: "Knock-out!" + Hammer Man tumbling off with a K.O. stamp, or "Hammer Man wins this round".
- Tests: unit 199/199 (+2 boss, +1 storage), tsc clean, e2e mobile 16/16 (+1 boss test), e2e desktop 16/16.
- Commits: 915dd6b (Ninja Sprint), fdbde37 (Boss Battle) — both on main.
- Next: P3 Balance the Scales (new topic + scales visual) — no owner input needed.

## 2026-09-06 05:40 (hourly routine, cloud)
- Backlog P3 done: **Balance the Scales** — new `scales` visual (`{ type: 'scales', left, right }`: beam + fulcrum SVG, two hanging pans, pillar) in `visuals.ts`/`style.css`; `balanceQ()` helper + three topics: `r-balance` (objects on one pan, `?` on the other; d3 `a = b + ?` with objects), `y1-balance` (d1 `a + b = ?` / `? = a + b`; d2 `a + b = c + ?`; d3 adds `a − b = ? + c`, `a + b = ? − c`, all within 20), `y2-balance` (d1 within 20; d2 2-digit ± ones/tens vs `c + ?`; d3 adds `n × t = ? + c` for 2/5/10 tables and `a + b = ? − c`, within 100). Distractors include the classic mistake (answer = left total) and the other operand.
- Tests: targeted unit test evaluates both sides of every balance prompt with `?` filled in (200 × 3 difficulties × 3 topics) and checks year ranges; e2e visuals test now also opens `y1-balance`, expects two pans and scores via the hook.
- Fixed a fragile unit test: endless-session test sliced `answer` for sequence questions (Order Up), silently losing lives before the deliberate wrong slices; it now slices sequences letter by letter and asserts full lives first.
- Docs: `docs/CURRICULUM.md` lists the three topics.
- e2e robustness: the random-pool tests (Sky Storm, Sprint, Boss) sliced `answer` once per question, which hangs on an Order Up / spelling sequence and, when the headless renderer stalls and a wave is missed, the "3 wrong slices" loop waited on a game that had already ended (seen once here: stuck at Q6 "Is it raining_"). New `solveCurrent()` / `waitForWrongOrEnd()` helpers slice sequences letter by letter and stop when the game ends.
- Commit: c54d6db (main).
- Tests: unit 209/209, tsc clean, e2e mobile: full run 14/16 before the e2e fixes (both failures analysed above), then the 4 touched tests 8/8 with `--repeat-each 2`. Desktop e2e not run (time).
- Skipped (owner input): #1 playtest, #2/#3 owner art, #4 bubble skins.
- Next: P4 Memory Match (non-slice card mode; needs a new screen, no owner input) or Next #9 (Y1/Y2 measurement & statistics topics).

## 2026-09-06 06:40 (hourly routine, cloud)
- Backlog P4 done: **Memory Match** — `src/game/memory.ts` (pure `Memory` class: flip/hide/match, turns, streak bonus score, stars by turns-per-pair, coins = 2×pairs + 5×stars; `THEMES` decks per island with distinct faces: R count/shapes/words, Y1 words/coins/2-D shapes/doubles, Y2 words 21–99/coins incl. £1 £2/3-D shapes/2-5-10 tables) + `src/ui/memory.ts` (DOM card grid with 3-D flip, each face spoken on flip, toasts, results modal with stars/coins/stickers/streak, `__sna.cards()/flip()/state()` hooks). `storage.memory` counts boards per year (`recordMemory`); island screen gets a "Memory Match" button.
- Backlog P5 done (and Next #10 as the same feature): **Story Sentences** — `sentenceQ()` in `writing.ts` builds word-bubble sequence questions (`r-sentence`, `y1-sentence`, `y2-sentence`; 3–7 words, 1–3 decoys, never a repeated label). Reception always sees the sentence on the card; Y1/Y2 see it at d1, then listen-and-build (picture emoji only). Generic curriculum test now accepts space-joined sequences; targeted test checks word uniqueness, capital/end mark, decoys and length.
- Test hygiene: pool-based session tests (sprint, boss) sliced `answer` on whatever the seeded pool drew; they now use a `solve()` helper that slices sequences item by item, so adding topics can't break them.
- Tests: unit 227/227, tsc clean; e2e mobile full run 17/17 after Memory Match; after Story Sentences the 6 sequence-affected tests (sentence, spelling, Storm, Sprint, Boss, Memory) 6/6 and sentence+Storm ×2 4/4 (Storm was flaky once on the first run, passed on retry — same renderer timing as before). Desktop e2e not run (time).
- Commits: cc84fea (Memory Match), 8401e7e (Story Sentences) — main.
- Skipped (owner input): #1 playtest, #2/#3 owner art, #4 bubble skins, P9 costumes (art).
- Next: P6 Sound Hunt (phonics by ear, no owner input) or Next #9 (Y1/Y2 measurement & statistics topics); P7 adaptive practice after that.

## 2026-09-06 07:40 (hourly routine, cloud)
- Backlog P6 done: **Sound Hunt** — `r-soundhunt` (d1 phase 2 sets 1–4, d2 all single-letter sounds + qu/x, d3 phase 3 digraphs/trigraphs) and `y1-soundhunt` (d1 phase 3, d2 phase 5 alternatives ay/ou/ie/ea/oy/ir/ue/aw/wh/ph/ew/oe/au, d3 + split digraphs a-e/i-e/o-e/u-e). Three keyword words are spoken ("Listen: sun, sock, sad. Which sound do they start with?"), the card shows only "🔊 Listen!" + a position hint, no visual. Decoys never share the answer's phoneme family (c/k, ai/ay/a-e, ee/ea, oi/oy, ur/ir/er, or/aw/au, oo/ue/ew, w/wh, f/ph). New `Question.listen`: with read-aloud off the card shows the three words instead (play.ts).
- Backlog P7 done: **Train with Sensei** — `src/game/sensei.ts` `weakestTopics()` (accuracy ↑, stars ↑, plays ↑; unplayed topics fill in; never tracing). Island button "Train with Sensei" (icons of the 3 picked topics, session count) starts a normal 5-stage mission over that pool; the card names each question's topic. `Session.byTopic` tallies hits/tries per topic in every mode and results call `recordAccuracy()` (new `TopicProgress.hits/tries`, old saves fall back to stars); `storage.training` counts completed sessions.
- Housekeeping: deleted `undefined/*.png` (2.6 MB of screenshots committed by the 04:40 run with an unset output dir).
- Tests: unit 239/239 (+7 Sound Hunt, +3 sensei, +1 session, +1 storage), tsc clean, e2e mobile 19/19 after Sound Hunt and 20/20 after Sensei (full runs). Desktop e2e not run (time).
- Commits: 58f335a (Sound Hunt + cleanup), b9da44a (Sensei) — main.
- Skipped (owner input): #1 playtest, #2/#3 owner art, #4 bubble skins, P9 costumes (art).
- Next: P8 Dojo challenges (daily 3 missions, bonus coins, streak multiplier — no owner input) or Next #9 (Y1/Y2 measurement & statistics topics); P10 Ninja Duel after that.

## 2026-09-06 08:40 (hourly routine, cloud)
- Backlog P8 done: **Daily Dojo** — `src/game/dojo.ts` (pure): 14-challenge pool in three groups (volume: answer 15/20/25 right · mode: 2 missions / Sprint / Boss KO / Storm 80 / Memory board / Sensei · focus: 5-combo / 3 stars / no slips / 6 writing / 10 maths), three picked per day from a date-seeded rng (same set for every island), `applyEvent()` moves progress from a finished game's result (capped at the goal), pays +10 🪙 per challenge once and +25 🪙 for the set, multiplied ×1.25 per consecutive completed dojo day (carried streak, cap ×2). `storage.dojo` rolls over per day (streak/lifetime total survive), `dojoToday()`/`recordDojo()`. Sky map gets a Daily Dojo card (progress bars, ✓ + bonus, streak pill, set-bonus footer); play and Memory results show "Dojo challenge done!" / "Daily Dojo complete!" rows; bonus coins go through `addCoins` so stickers unlock. The session's own `.coin-gain` stays separate.
- Backlog P11 done: **Mission certificate** — `src/ui/certificate.ts`: `certificateText()` (pure wording, en-GB long date) + `drawCertificate()` (1200×850 canvas: sky, parchment card in the avatar's glow colour, auto-fitted name, mission/island line, stars, accuracy, date, avatar art) + `shareCertificate()` (Web Share with files on phones, else download). "🎓 Certificate" button on the results of a won mission / Sensei session; `__sna.certificate()` returns the PNG data URL. Rendered and eyeballed in headless Chromium (Fredoka falls back to the system font there because Google Fonts is blocked in the sandbox; on devices the web font is used).
- Next #15 done: **Haptics** — `haptic(kind)` in `audio.ts` with patterns for slice / correct / wrong / life / stage, gated by the sound toggle, never throws; wired to swipe hits, wrong slices, lost lives and stage clears.
- e2e fixes: the full-mission test now adds today's dojo bonus to the expected coin total (bonuses ≤ 55 so the sticker count stays 3); `waitForWrongOrEnd` waits for a real decoy in flight — at Q6 of Sky Storm only the target and the TNT bubble may be launched, and `wrong()` refuses the bomb (failed twice in the full run before the fix, 2/2 after).
- Tests: unit 248/248 (+6 dojo, +2 certificate, +1 haptics), tsc clean; e2e mobile full run 19/21 before the two test fixes, then the affected tests (full mission, Storm, Dojo) 6/6 with `--repeat-each 2`, full mission again 1/1 with the certificate, play-path smoke after haptics (see commit). Desktop e2e not run (time).
- Commits: e1a7b9b (Daily Dojo), cf2d748 (certificate), 33d84d1 (haptics + worklog) — main.
- Skipped (owner input): #1 playtest, #2/#3 owner art, #4 bubble skins, P9 costumes (art).
- Next: P10 Ninja Duel (two-player split screen — sketch: two `Arena`s stacked on one canvas or two canvases, same question, first correct slice wins the point, best of 10; needs a duel HUD and a pure `Duel` scorer) — a full run's worth; alternatives if time is short: Next #9 (Y1/Y2 shape, measurement, statistics topics), #13 PWA.
## 2026-09-06 08:00–09:40 (interactive session with the owner)
- Reviewed the night's runs; unit 239/239, tsc clean, e2e mobile 20/20 + desktop 19/20 (1 flaky: gentle-reception wave took >30 s to fall under software rendering, passed on retry). Artifact republished from 029eeb7.
- Owner prioritisation (AskUserQuestion): top items = playtest, Dojo, Y1/Y2 shape-measure-statistics, parent dashboard; Y3–Y6 by the routine in the background; reward system judged insufficient (nothing to spend coins on); routine continues hourly, no Vercel.
- Playtest round 1 fixes → 85711b9: **outcome beat** (`arena.reveal()`: frozen wave, ✓/✗ spotlight, ghost bubble, card fills the answer via `fillAnswer`, HOLD 1 s/1.8 s/1.5 s + gap; sprint brisk), **batched waves** for long sentences (no overlap; next word always in the first batch), Master Ninja art → `public/avatars/sensei.webp`. Tests: unit 241, e2e mobile 22/22.
- Code review (separate subagent; debt / performance / maintainability): 20 findings → issues #26–#45.
- **Backlog moved to GitHub Issues** (#1–#47, order in #46, labels + scrum checklist in every issue: Develop → Review/QA by a different agent → Owner action → Done). `BACKLOG.md` is now a pointer; new routine prompt in `docs/ROUTINE-PROMPT.md` (owner must paste it into the routine and set `GITHUB_TOKEN` on it).
- Open owner feedback captured as issues: Trace Words too lenient (#4), British English audit (#47), Lessons concept (#10, plan together later), Master Ninja role = Sensei + unlockable avatar (#5), coin shop scope (#6).
- Next (this session): #4 tracing fix, review/QA of 85711b9 by a fresh subagent, then #5.
- 10:15 — #4 Trace Words: per-letter masks (`mask[i] = letter+1`), pure `scoreTrace()` (every letter ≥ 55 %, word ≥ 65 %, outside ≤ 45 %), hint names the skipped letter, `autoTrace(letters?)` hook, Tracer removes its canvas listeners (review #39). Unit +5, e2e +1 (all-but-last letter must fail). Rebased onto the 08:40 run (Dojo, certificate, haptics → issues #7/#17/#19 closed, QA pending by the next run).
- 10:45 — Independent review of 85711b9 + adf3a39 by a fresh subagent (REQUEST CHANGES, all addressed): full rows use the whole width and three bubbles always fit (no phone overlap), slower batch gap on narrow screens, a stroke stops at the first hit once frozen, ghost bubble avoids the ✗ bubble, Sound Hunt keeps its listen words during the reveal, gentler miss wording ("It flew away!"), toasts no longer repeat the answer, y2-trace never serves a one-letter word, e2e reads the expected answer before slicing, `Arena.flash()` deleted. Full e2e on adf3a39: 48/48 (mobile + desktop); mobile 24/24 after the fixes. Artifact republished.
- Note for the next run: the artifact viewer blocks plain download links, so the certificate's "download" (cf2d748) does nothing inside the artifact (share sheet may still work) — needs the `downloads` capability or a share-only path there; open an issue. b9f990c was committed in the cloud while the Mac was unreachable — push pending.

## 2026-09-06 09:40 (hourly routine, cloud — first run on the GitHub-Issues workflow)
- Review/QA: no open `claude/issue-*` PRs. Main (adf3a39, #4 Trace Words fix from the owner session) re-checked: unit 255/255, tsc clean; the full mobile e2e run below also covers it (tracing test passed).
- Developed **#5 Master Ninja** → PR #49 (`claude/issue-5`, a4d0d03): Sensei portrait on the *Train with Sensei* button, in the first-play tutorial and as the speaker of the training results (calm won / try-again lines, `senseiLine()`); playable `master` avatar as a locked 11th card (`ALL_AVATARS`, `masterProgress()`: every topic on every island ≥ 1 star, progress `n/65 topics ★`, tap says the rule and shakes); `fx: 'master'` mixes every element's particles and slice sounds. Acceptance criteria written into #5, Refine + Develop ticked; Review/QA left to the next run (never review your own PR).
- Tests: unit 258/258 (+3 avatars.test.ts), tsc clean, build ok; e2e mobile full run 24 passed + 1 flaky (sentence batch overlap — physics timing under software rendering, passed on retry), then 4 affected tests 4/4 after the final CSS tweak. Desktop e2e not run (time). Screenshots taken of all four Sensei placements (not committed).
- GitHub access from the routine works through the GitHub MCP tools (issue comment, issue update, PR) — no `GITHUB_TOKEN` action needed from the owner.
- Next run: review/QA PR #49 (desktop e2e + screenshots for the owner), then #8 (Y1/Y2 shape, measurement and statistics topics). #6 (coin shop) is `owner-input`; #1 needs the owner.

## 2026-09-06 11:00–12:30 (interactive session with the owner, continued)
- PR #49 (Master Ninja, #5, opened by the 09:40 run) reviewed by a fresh subagent (APPROVE, 3 low findings fixed in 0e45a8d), QA: unit 258/258, tsc, e2e 50/50 → squash-merged as 1771a4f; artifact republished (label "1771a4f — Master Ninja"). Screenshots sent to the owner for the visual approval (owner action still open on #5).
- #47 British English audit → PR #52: TS-AST scan of every string in src/** + index.html and every generated question against a KS1 blocklist; no offenders. Reviewer requested changes (tsc break, nested templates) → fixed → approved → 6c311af. Not player-visible, artifact unchanged.
- New issues: #50 certificate download does nothing inside the artifact viewer (`downloads` capability), #51 sticky "Let's go!" covers the last avatar row. #46 updated (#5, #47 ticked).
- Commit identity decision (owner): commits stay under the owner's name/e-mail; Claude credited via Co-Authored-By.
- Next: #6 coin shop infrastructure (routine-ok part), #48 tap-to-pop projectile (owner approves visuals), review #26/#27 before Dojo/Duel/Y3+.

## 2026-09-06 13:40 (hourly routine, cloud)
- Review/QA (reviewer role, both PRs by the 11:00–12:30 owner session): **PR #57** (Android APK, #53) — unit 258/258, tsc, full e2e 50 passed + 2 desktop flaky (retry-pass), Actions run #2 built the APK. **Changes requested**: the popstate handler's `else nav.map()` branch never resets `fromPop`, so after one back to the map the next island visit pushes no history entry and the following back press leaves the app (reproduced with a Playwright probe: `history.length` stays 3). Fix + a stronger e2e suggested in the PR. **PR #58** (five-frames, #54) — unit 261/261, tsc, e2e mobile+desktop 6/6 on the affected tests, screenshots look right (frames 336×34 px on 390 px). **Changes requested**: 26 generated `android/` files (`cap sync` output that #57 git-ignores) were swept into the commit; remove with `git rm -r --cached android`. GitHub refuses REQUEST_CHANGES from the same account, so the verdicts are COMMENT reviews headed "CHANGES REQUESTED". Do not fix them in this run (rule) — the next run re-reviews.
- Developed **#6 coin shop infrastructure** → PR #59 (`claude/issue-6`, 60bc9d5): wallet in the save (`spent`/`owned`/`equipped`, balance = lifetime coins − spent so stickers still unlock), pure `src/game/shop.ts` (catalogue, canBuy/buy/equip/equippedItem), `src/ui/shop.ts` Ninja Shop off Rewards (Buy/Equip/Equipped ✓, Coming-soon cards per kind), one placeholder **Golden Trail** (150 🪙) via a new `Arena.trailCore` option; the top-bar pill shows the balance, Rewards shows coins earned. Acceptance criteria written into #6, Refine + Develop ticked. Tests: unit 269/269 (+8), tsc, build, e2e mobile shop test 1/1 (+ full-mission/rewards path). Screenshots of Rewards/Shop taken (not committed). Owner action: approve the Golden Trail look + price.
- Env notes: two Playwright runs cannot share port 4173 (`reuseExistingServer`) — use a copied config with another port when running e2e in a worktree; `page.reload()` after `/?reset=1` wipes the save (use `goto('/')`).
- Next run: re-review #57 and #58 once fixed (small fixes), review/QA #59 (full e2e + attach the shop screenshots for the owner), then #48 (tap-to-pop projectile) or #55 (mission progress bar).

## 2026-09-06 12:30–15:15 (interactive session with the owner, continued)
- Owner requests → issues #53 (APK, top priority), #54 (five-frames), #55 (progress bar), #56 (schools/classes/leaderboard — parked, backend decision later). #46 rebuilt with everything present.
- **#53 Android APK → PR #57**: Capacitor wrapper, GitHub Actions "Android APK" (artifact + v* releases), history-based back button, icons/splash, docs/ANDROID.md. Review (independent subagent) requested changes (release permissions, dead back presses, keystore caveat) → fixed (ecbe7d6) → APPROVE; Actions green (76040cd, ecbe7d6) → merged 1f37366.
- **#54 five-frames → PR #58**: objects in rows of five with faint slots. Review caught stray `android/` files in the commit (branch rewritten) + short-screen sizing → fixed → APPROVE → merged 4569a51. Full e2e on main 51/52: the desktop real-swipe test flaked twice because edge-to-edge rows put bubbles r+8 from the side and the stroke started off-canvas → helper fixed (3ff55c7, 8/8 repeats). Artifact republished from 4569a51.
- **#6 shop → PR #59** (opened by the 13:40 routine): reviewed, fixes pushed (1e53016: history router, "Use/Using ✓", 44 px buttons, tolerant wallet). Waiting for the owner's approval of the Golden Trail + shop screenshots before merge.
- Session-limit note: a review subagent hit the account's session limit once (reset 13:30 UTC); re-run after the reset.

## 2026-09-06 14:40 (hourly routine, cloud)
- Review/QA (reviewer role): **PR #59** (coin shop infrastructure, #6, by the 13:40 run) merged onto main (after #57/#58) — unit 272/272, tsc, build; full e2e mobile + desktop 52/54: `real swipe` desktop = the known software-rendering flake (fails once on an unrelated branch too, passes on retry), but the **desktop `ninja shop` test times out every time (4/4)**: it navigates four times and each navigation costs ~12.5 s in the sandbox because the Google Fonts stylesheet blocks until the proxy gives up (measured `load` 12.7 s / `domcontentloaded` 12.5 s); mobile scraped through at 57.7 s. **Changes requested** (COMMENT review headed CHANGES REQUESTED): `test.slow()` or seed `spent: 100` instead of a second reload. Non-blocking: the Shop pushes no history entry, so the hardware back button jumps to the map instead of Rewards (#57 landed after the PR); suggested a `shop` nav entry. Code otherwise meets all five acceptance criteria. Rewards/Shop screenshots taken and sent to the owner in the session.
- Developed **#48 tap-to-pop projectile** → PR #60 (`claude/issue-48`, f9f74a6 + 1570707 — the helper split had dropped `swipeAnswer`'s on-canvas check from main; restored, 4/4 on both projects): a tap (pointer down / `hitLabel`) throws a `Shot` from the bottom of the arena that pops the bubble on landing after 150 ms; scoring, reveal and hooks settle at the tap, swipes unchanged; `SHOT_STYLE` per element (shuriken ×3 incl. golden Master, fireball, orb, bolt, rock, leaf, shard, star, laser) with an element wake; `sfx.whoosh` at the throw, element slice sound + haptic at landing; `__sna.state().shots`. Acceptance criteria written into #48, Refine + Develop ticked. Tests: unit 266/266 (+2 arena.test.ts), tsc, build, e2e mobile full run 27/27 (new real-tap test; `swipeAnswer` shares a `frozenTarget` helper). Sheet of all 11 projectiles mid-flight sent to the owner (arena paused 50 ms after `answer()`); owner approves the looks before merge.
- Env notes: a second Playwright run needs its own port (copied config with 4174); a screenshot script must live inside the repo for `@playwright/test` to resolve; ~12.5 s per navigation from the blocked Google Fonts request is the main e2e cost here (idea for #32: abort `fonts.googleapis.com` in the Playwright config).
- Owner action open: #5 Master Ninja visuals, #6 Golden Trail look + price (PR #59), #48 projectile looks (PR #60).
- Next run: re-review #59 once the test is fixed; review/QA PR #60 (desktop e2e + screenshots); then #55 (mission progress bar) or #50 (certificate download in the artifact viewer).
- 15:15–17:00 — Owner reported three things; all became issues (#62 Story Sentences in pool modes, #63 Memory Match card, #64 Android stable keystore, #65 no-speech devices plan) and #62/#63 shipped as PR #66 (306b730).
  - **#62 diagnosed by measurement, not guesswork**: overlap was fine everywhere (1.3–2.1 × radii); the fault was launch *order* — `spawnWave` pulled only the current target into the first batch and scattered the rest of the sentence at random, e.g. `[330, 4510, 0, 3520, 660, 4180, 7040] ms`, so the child waited ~4.5 s per word and early-arriving words fell unused. Standalone play looked fine because stages 1–2 are d1 (sentence shown, 1–2 batches) while Sprint/Boss/Storm reach d2 in 4–8 questions and Sensei mixes stages 3–5. Fix: `dealOrdered()` deals the remaining sequence across batches in order, `rush()` pulls the next word up when earned, and sequence waves carry ≤4 bubbles per flight on any screen (a tablet used to put all 10 up in one 4-second flight).
  - **#63**: the Memory Match *screen* rule was on the bare `.memory` class, which the island button also carries → that card was 844 px against 86 px for the others. Scoped to `.screen.memory`.
  - **Frame-rate regression found while fixing the desktop suite**: the game ran at **0.17× real time on 1280×800 and 0.67× on a phone** because `.bg-play::after` animated `background-position` over 13 gradients under the live canvas. Now 0.99× / 1.00× (first item of #30). This is why `reception is gentle` had been flaking all day.
  - Review by a fresh subagent (numerically simulated the new e2e and found it 11–19 % flaky on desktop, plus a `rush()` overlap risk) → all findings fixed → APPROVE. Full e2e 54/54, unit 266/266, tsc clean. Artifact republished from 306b730.
- Still open: PR #59 (shop, owner's visual approval), PR #60 (#48 ninja star — review requested changes: tapping TNT explodes twice and the bomb keeps flying 150 ms).

## 2026-09-06 18:00 (hourly routine, cloud)
- Review/QA (reviewer role — different agent from the developers): **PR #60** (#48 tap-to-pop, by session 01GVGV…) — the "changes requested" from the owner session was only in WORKLOG, never posted on the PR, so posted a COMMENT review headed CHANGES REQUESTED. **Confirmed the bug by reading the code**: on a tap, `hitBubble(b,false)` calls `throwAt(b)` before `onHit`; the `BOMB` branch in play.ts bursts red at once but never marks the bubble dead nor suppresses the star, so `update()` keeps the exploded TNT flying for `SHOT_FLIGHT` (150 ms) and `landShot` bursts it a second time + fires the avatar slice sound/haptic (`onLand`). Suggested: pop the bomb instantly on tap (no star), guard `landShot`/`onLand` against an already-dead unmarked target, and add an e2e that taps a 💣. Fast checks on the branch green (tsc, unit 266/266, build); skipped the QA e2e budget until the fix lands. **PR #59** (#6 shop) — already carries a standing CHANGES REQUESTED (desktop `ninja shop` e2e times out) with no new commits; nothing to re-review. Both PRs also blocked on owner visual approval, so no merge this run.
- Developed **#67 arena width cap** → PR pending (`claude/jolly-gauss-e88vs5`, 8353ffe — this session's designated push branch, so the branch name is not `claude/issue-67`; the PR still says `Closes #67`). Cap `#arena` + `.hud` to a centred `--arena-w` box (600px default); the fixed sky shows through the transparent canvas and continues outside, `Arena.W/H` follow the box (so the phone batching geometry applies everywhere), Hammer Man corner + HUD stay inside the arena, subtle frame (hairline+vignette+shadow) only above the arena width so phones are pixel-identical. e2e: `swipeAnswer` now offsets `page.mouse` by the canvas box (bubbles() are canvas-space coords), + a new capped/centred assertion. Acceptance criteria were already written into #67 by the owner.
- Tests: `npx tsc --noEmit` clean, unit **266/266**, build ok. e2e focused set on **both** projects 6/6: new #67 test, "long sentences launch in batches", and "real swipe slices" (the last proves the canvas-offset fix on desktop with the centred canvas). Full suite left to the reviewer (QA). Desktop 1280×800 screenshot taken (bounded, centred arena; sky both sides) — sent to the owner for the width/framing approval.
- Note: WORKLOG kept on the PR branch (not pushed to main directly) because this session's designated branch guardrail forbids pushing elsewhere; it reaches main when #67 merges.
- Next run: re-review PR #60 once the TNT fix lands (then full e2e QA); review/QA this #67 PR (full mobile+desktop e2e + screenshot check) — a *different* agent; #59/#60 still need owner visual approval. Then #65 (no-speech plan) or #8 (Y1/2 shape/measure/statistics).

## 2026-09-06 18:45 (hourly routine, cloud)
- Review/QA (reviewer role, a different agent from the developers): **PR #59** (coin shop, #6) — re-reviewed `57c7cb0`, which lands the fixes for the standing CHANGES REQUESTED: the desktop `ninja shop` e2e now passes on **both** projects (mobile 45.0 s, desktop 53.8 s — was timing out 4/4) via `test.slow()` + three navigations instead of four; `tsc` clean, unit 272/272, build ok. Posted a **QA PASS** comment. Only remaining blocker: **owner visual approval** of the Golden Trail look + price, so not merged (no new visual ships without sign-off). The full 54-test suite still can't finish in the 9-min budget here — documented sandbox cost (blocked Google Fonts ~12 s/nav), not a regression. **PR #60** (#48 tap-to-pop) — standing CHANGES REQUESTED (TNT explodes twice) unaddressed, no new commits; nothing to re-review, and still blocked on owner visual approval. #67 confirmed merged (as #68).
- Developed **#50 certificate download in the artifact viewer** → **PR #69** (branch `claude/jolly-gauss-t5jzke`, this session's designated push branch; `Closes #50`). Pure `certRoute()` picks share → artifact `downloads` save (`claude.use('downloads').save`) → full-screen "press & hold" fallback → `<a download>`; `deliverCertificate()` degrades gracefully (declined → friendly toast, other errors → full-screen, cancelled share → step down); new `.cert-view` overlay. Tests: unit 270/270 (+4 route tests), tsc clean, full-mission e2e on **both** projects taps 🎓 with a stubbed `window.claude` (asserts the save prompt + the fallback). `dialog` added to the British-English scanner's API-literal allowlist (ARIA role). Docs (ROUTINE-PROMPT.md + CLAUDE.md): republish the artifact with `capabilities: {downloads: true}`. Fallback screenshot sent to the owner. Refine + Develop ticked in #50.
- Note: WORKLOG kept on the PR branch (guardrail forbids pushing to main directly); it reaches main when #69 merges.
- Owner action open: #5 Master Ninja visuals, #6 Golden Trail look + price (PR #59, now QA-clean — ready to merge on approval), #48 projectile looks + TNT fix (PR #60), #50 try the certificate in the artifact on the phone (PR #69).
- Next run: review/QA **PR #69** (#50 — a *different* agent: full e2e both projects + read the diff); merge **PR #59** if the owner has approved the Golden Trail; #60 still awaits the developer's TNT fix. Then #65 (no-speech plan, owner-input) or #8 (Y1/2 shape/measure/statistics).

## 2026-09-06 19:40 (hourly routine, cloud)
- Review/QA (reviewer role, a *different* agent from the developer): **PR #69** (#50 certificate delivery, by session 013uKB…) — read the diff against the acceptance criteria, `npx tsc --noEmit` clean, `npm test` **270/270** (+4 route tests), `npm run build` ok, and the full-mission e2e (which taps 🎓 with `canShare` forced off and asserts both the `downloads` save prompt and the `.cert-view` fallback) green on **mobile (1.3m) + desktop (1.5m)**. `certRoute` pure and fully unit-tested; graceful degradation confirmed; the full-screen fallback reuses the already-approved certificate art (functional chrome, not new game art) — screenshot sent to the owner. **APPROVE → squash-merged as `e9e6974`**; #50 closed, Review/QA/Done ticked, #46 updated (#50 + #67 ticked — #67 merged earlier as #68). **Owner/publisher action:** republish the artifact with `capabilities: {downloads: true}` so the in-viewer save prompt works (without it the phone shows the "press and hold" fallback).
- **PR #59** (#6 shop): the standing CHANGES REQUESTED is already fixed on the branch head (`57c7cb0` — desktop `ninja shop` e2e via `test.slow()` + three navigations), and the owner has approved the Golden Trail look + price 200. **Only remaining item: change the price `150 → 200` in `src/game/shop.ts` + the e2e balance assertions** — an owner-directed dev turn on the `claude/issue-6` branch. Not done this run: my designated push branch is `claude/jolly-gauss-mufy0f`, and the guardrail forbids pushing to `claude/issue-6`. **This is a workflow gap the owner should know about** — routine runs develop on their own designated branch, so nobody picks up the small finishing change on another run's issue branch. Suggest the owner (or an interactive session on `claude/issue-6`) makes the two-line change, then a fresh agent merges. **PR #60** (#48 tap-to-pop): standing CHANGES REQUESTED (TNT explodes twice) still unaddressed, no new commits; nothing to re-review, still blocked on owner visual approval.
- Developed **#51 avatar screen: last card row under the sticky Let's go! button** → **PR #70** (branch `claude/jolly-gauss-mufy0f`, this run's designated push branch; `Closes #51`). Root-caused by measurement: at max scroll the name-row already gave ~75 px clearance, but while a child browses down to Master the pinned `#go` (z-index 2) floats over the last row and intercepts taps. Fix: bottom-padding runway (`calc(84px + var(--sab))`) on `.avatar-screen` + `revealCard()` — tapping a covered card (incl. the locked Master shake) scrolls it into view. New e2e at **390×664** asserts the last card ends clear of `#go`; green on mobile + desktop; unit 270/270, tsc clean, build ok. Screenshot (Master tapped from the top, fully revealed) sent to the owner. Acceptance criteria written into #51, Refine + Develop ticked. Merged current main into the branch so this WORKLOG entry + #69 travel together and #70 stays conflict-free.
- Artifact NOT republished by the routine (my scheduled prompt lists "publish artifacts" under Do-NOT); the #50 fix reaches players only once the owner republishes with `capabilities: {downloads: true}` — flagged above and on #50/#69.
- Owner action open: **#50 republish the artifact with `capabilities:{downloads:true}`** (unlocks the certificate save in the viewer); #6 make the `150→200` price change on PR #59 then merge; #5 Master Ninja visuals; #48 projectile looks + TNT fix (PR #60).
- Next run: review/QA **PR #70** (#51 — a *different* agent: full e2e both projects + eyeball the avatar screen at 390×664); if the #59 price change has landed, review/QA + merge it; #60 still awaits the developer's TNT fix. Then #8 (Y1/2 shape/measure/statistics) or #65 (no-speech plan, owner-input).

## 2026-09-06 20:39 (hourly routine, cloud)
- Review/QA (reviewer role — a different agent/session from the developer): **PR #70** (#51 avatar last-row fix, by session 01KJxTdD…) — `tsc` clean, unit **270/270**, build ok, e2e **8 passed** on mobile + desktop incl. the new `#51` test; 390×664 screenshot (scrolled to bottom) confirms the Bolt/Master row is fully clear of the sticky "Let's go!" button. Layout/scroll only, no new art → no owner approval needed. **QA PASS → squash-merged as `ad9d9aa`**, #51 closed, #46 ticked. PR #69 (#50 certificate) confirmed merged earlier. **PR #59** (#6 shop) and **PR #60** (#48 tap-to-pop) unchanged since their last reviews and both still blocked on the owner's visual approval (Golden Trail price/look; TNT-double-explode fix on #60 still owed by its developer) — nothing to re-review, not merged.
- Developed **#8 Y1/Y2 measurement & time** → PR pending (`claude/jolly-gauss-4ig4t6`, this session's designated push branch; `Closes #8`). Nine new text/spoken maths topics (no new visuals, so no owner art needed): **Y1** `y1-length` (Long & Tall — compare length/height, cm in the hint), `y1-mass` (Heavy & Light, g), `y1-capacity` (Full & Empty, ml), `y1-months` (day/month order, days in a week/weekend, seasons); **Y2** `y2-length` (compare, sensible unit cm/m, 1 m = 100 cm, add lengths), `y2-mass` (g/kg, 1 kg = 1000 g), `y2-capacity` (ml/l, 1 l = 1000 ml), `y2-temp` (compare warmer/colder, read °C), `y2-duration` (durations: minutes/hours/days/weeks/months, month order). Shared `measureCompare()` (d1 = 2 things comparative, d2/3 = 3 things superlative) + `unitChoice()`; every question carries a `say` line for non-readers and shows the measured values in the hint.
- Tests: `npx tsc --noEmit` clean, unit **299/299** (+29: the generic suite auto-validated all 9 topics × 3 d, plus 2 targeted tests — duration/month/day facts, and that the compare topics slice the correct extreme). British-English scanner green (centimetres/metres/grams/kilograms/millilitres/litres). Build ok. Gameplay screenshot of `y1-length` verifies the topic plays (the stray "3" bubble in it is the first-play tutorial demo from `?reset=1`, not an option). Full mobile e2e running at push time — result recorded on the PR.
- **Scope note on #8**: this delivers the "measurement (length, mass, capacity)" + temperature + time-durations/months gaps in full. **Position/direction** (turns, left/right) and **Y2 statistics** (pictograms, tally, block diagrams) are deliberately left for a follow-up because they want a drawn visual (owner art / a new `visuals.ts` case + e2e) — noted in the PR and in docs/CURRICULUM.md, so #8 should stay open after this merges until those land.
- Note: WORKLOG kept on the PR branch (the designated-branch guardrail forbids pushing to main directly); it reaches main when #8's PR merges. Artifact not republished (routine does not publish artifacts).
- Next run: review/QA this #8 PR (a *different* agent: full mobile+desktop e2e + read the diff + a few topic screenshots); #59 (merge once the owner approves the Golden Trail price/look), #60 (awaits the developer's TNT fix). Then the #8 follow-up (statistics/position visuals) or #9 (parent dashboard).

## 2026-09-06 22:05 (hourly routine, cloud)
- Review/QA (reviewer role, a *different* agent from the developer): **PR #71** (#8 Phase 1 — Y1/Y2 measurement & time topics, by session 01D6GVcy…). Read the diff against #8's acceptance criteria + CLAUDE.md: pure additive change (9 new maths generators + their `MATHS_TOPICS` registry entries + two shared helpers + targeted tests), no touch to arena/play/`__sna` hooks/visuals. Correctness confirmed (answers ∈ options, distractors deduped/clamped, unit conversions + duration/month/day facts right, every topic has a `say` line, comparisons show values in `hint`), British-English throughout. Tests on `a1aa94d`: `tsc` clean, unit **299/299**, build ok, full e2e **mobile + desktop 58/58** (12.2m). Posted a QA-PASS review (COMMENT — the shared token can't self-approve) and **squash-merged as `2f4e03d`**. #8 **kept open** for Phase 2 (statistics + position/direction, which need a visual); Review/QA/Owner-action ticked, comment left with the hash.
  - PRs **#59** (#6 shop) and **#60** (#48 tap-to-pop): no new commits since the prior runs' reviews — both still blocked on owner visual approval (and #60 on the developer's TNT double-explode fix, #59 on the `150→200` price change that lives on `claude/issue-6`). Nothing new to re-review this run.
- Developed **#9 Parent dashboard behind a grown-ups gate** → PR (branch `claude/jolly-gauss-pq5l7e`, this run's designated push branch; `Closes #9`). New pure module `src/game/parents.ts` (grown-ups gate = a 6–9 × 6–9 times-table question a young child can't do; `parentSummary()` aggregates the stats already in the save — per-topic accuracy from `hits`/`tries`, stars, weakest/strongest topics, per-year totals, per-mode bests) and a read-only screen `src/ui/parents.ts` reached from a discreet "👤 For grown-ups" link in the map footer. Read-only, no accounts, no new game art (functional chrome only). Wired `nav.parents` + a `parents` history entry into `main.ts` (back steps to the map like Rewards). CSS reuses the existing tokens/panels/`isl-bar`.
  - **Time-on-task deliberately omitted** — it isn't recorded in the save today (adding it means touching the play loop); flagged in the PR + on the dashboard + as a small follow-up rather than widening this PR.
  - Tests: `npx tsc --noEmit` clean, unit **304/304** (+5 in `tests/unit/parents.test.ts`: gate range/answer, `checkGate` exactness, summary aggregation + weakest/strongest ranking with the `RANK_MIN_TRIES` threshold, `pct`), build ok, new e2e (`For grown-ups: …`) **green on both mobile + desktop** (wrong answer keeps the gate + warns; correct product opens the dashboard; back returns to the map). Full suite left to the reviewer (QA).
  - Screenshot of the seeded dashboard (390 px) sent to the owner. Acceptance criteria written into #9; Refine + Develop ticked.
- Note: WORKLOG kept on the PR branch (designated-branch guardrail forbids pushing to main directly); it reaches main when #9's PR merges. Artifact not republished (routine does not publish artifacts) — the new #8 measurement topics and this dashboard reach players only when the owner republishes.
- Owner action open: **#8** try the new measurement/time topics; **#6** #59 needs the `150→200` price change on `claude/issue-6` then merge (owner already approved the look — this is the standing workflow gap: routine runs push to their own branch, so nobody picks up a small finishing change on another run's issue branch); **#48** projectile looks + TNT fix (PR #60); **#5** Master Ninja visuals.
- Next run: review/QA **PR #9** (a *different* agent: full mobile+desktop e2e + eyeball the dashboard + gate); the #8 follow-up (Phase 2 statistics/position visuals) or #65 (no-speech plan, owner-input). #59/#60 still await owner action.

## 2026-09-06 22:35 (hourly routine, cloud — owner follow-up)
- Owner confirmed the #6 decision (Golden Trail look approved, **price 200**) and asked this run to finish #59 since no other session had picked up the finishing change. Done: on `claude/issue-6`, merged main in (11 commits — one `arena.ts` conflict resolved by keeping both the #62/#67 wave-batching fields and the shop's `trailCore`), set the Golden Trail **price 150 → 200** in `src/game/shop.ts`, and updated the shop unit + `ninja shop` e2e assertions (balance 200 − 200 = 0 after buying; lifetime coins still unlock stickers). QA on the merged head `f7906fa`: `tsc` clean, unit **307/307**, build ok, full e2e **mobile + desktop 60/60**. **Squash-merged PR #59 as `13cce97`**; #6 closed, workflow ticked, #6 ticked in #46. This resolves the standing workflow gap the earlier runs flagged.
- Then merged the new main (with the shop) back into **PR #72** (parent dashboard) to keep it mergeable: conflicts in `main.ts` (nav/popstate now carries both `shop` and `parents`; `rewards` re-renders on pop because the shop sits on top of it, `parents` is a leaf), `home.ts` (`Nav` type gains both `shop` and `parents`), and `style.css` (both CSS blocks kept). Re-validated below.
- Further shop skins / bubble skins / island decorations / costumes remain **owner-art follow-ups** (open a fresh issue when art is supplied) — #6 shipped the infrastructure + the one approved Golden Trail.

## 2026-09-06 23:00 (hourly routine, cloud)
- Review/QA (reviewer role, a *different* agent from the developer): **PR #72** (#9 parent dashboard, by session 01T4P2sQ…). Read the diff against #9's acceptance criteria + CLAUDE.md — pure additive, read-only, no accounts, **no new game art** (functional chrome reusing the existing tokens/panels/`isl-bar`); `parentSummary`/gate logic pure & correct (weakest = ascending accuracy with a tie-break on tries; ranking gated behind ≥5 tries), all child-supplied text escaped via `esc()`, British English throughout, nav mirrors the Rewards/Shop back pattern. Tests on `a6ecd3c` (branch current with main): `tsc` clean, unit **312/312**, build ok, full e2e **62/62 on mobile + desktop** (13.8m) incl. the new `For grown-ups` test; seeded dashboard eyeballed at 390 px. Posted a QA-PASS review (COMMENT — shared token can't self-approve) and **squash-merged as `e4bdb98`**; #9 closed, Review/QA/Done ticked, #46 ticked, owner-action comment left (try the dashboard on the phone). **PR #60** (#48 tap-to-pop): no new commits since the prior review — standing CHANGES REQUESTED (TNT double-explode) still owed by its developer + owner visual approval; nothing to re-review.
- Developed **#8 Phase 2a — position & direction** → PR (branch `claude/jolly-gauss-987jva`, this run's designated push branch; `Closes` nothing — #8 stays open for the statistics follow-up). Two new maths topics, **text + directional-arrow emoji only, no drawn `visuals.ts` case and no owner art** (same class as Phase 1, so mergeable without visual sign-off):
  - **`y1-position` "Left, Right & Turns"** (Y1 Geometry): name the way an arrow points, pick the arrow for a direction, and — d2/d3 — where you face after a whole/half/quarter (d3 adds three-quarter) turn clockwise/anti-clockwise.
  - **`y2-position` "Turns & Right Angles"** (Y2 Geometry): where you face after any of the four turns either way, rotation **as right angles** (quarter = 1 … whole = 4, both directions), and naming the turn from a start→end arrow.
  - Shared pure `turnEnd(start, steps, clockwise)` rotates round the four compass directions; every question carries a `say` line for non-readers and an on-card arrow.
- Tests: `npx tsc --noEmit` clean, unit **320/320** (+8: generic suite auto-validated both topics × 3 d, plus 2 targeted tests — `turnEnd` arithmetic, and that every generated turn/right-angle/arrow answer is correct by re-deriving it from the prompt). British-English scanner green (uses "anti-clockwise", "right angles", never "counterclockwise"). Build ok. Gameplay screenshot of `y2-position` at 390 px sent to the owner — arrow emoji render cleanly as bubbles; the prompt reads e.g. "Face ⬆️, then a quarter turn anti-clockwise. Which way now?".
- **Scope note on #8**: this closes the **position/direction** gap. **Statistics** (Y2 pictograms, tally, block diagrams) still wants a drawn `visuals.ts` case (+ e2e), so **#8 stays open** for that final follow-up; docs/CURRICULUM.md updated to reflect the split.
- Note: WORKLOG kept on the PR branch (designated-branch guardrail forbids pushing to main directly); it reaches main when the PR merges. Artifact not republished (routine does not publish) — the new topics reach players when the owner next republishes, alongside #9's dashboard and #8 Phase 1's measurement topics.
- Owner action open: **#9** try the grown-ups dashboard on the phone; **#48** projectile looks + TNT fix (PR #60); **#5** Master Ninja visuals; republish the artifact (with `capabilities:{downloads:true}`, #50) to ship #9 + #8 topics to players.
- Next run: review/QA this #8 Phase 2a PR (a *different* agent: full mobile+desktop e2e + read the diff); #60 still awaits the developer's TNT fix. Then #8 Phase 2b (statistics visuals) or #14 (phonics phases order) / #73 (guard-rail checks — owner filed it as "do first"; add it to #46 or confirm ordering with the owner).

## 2026-09-07 00:50 (hourly routine, cloud)
- Review/QA: only open PR is **#60** (#48 tap-to-pop) — no new commits since the 09-06 reviews (standing CHANGES REQUESTED: TNT double-explode fix owed by its developer + owner projectile-art approval). Nothing new to re-review. The #8 Phase 2a and guard-rail PRs already merged to main (e1e54dd, 27d9366).
- Developed **#27 review/debt — year ids & per-year assets hard-coded in seven places (blocks Y3–Y6)** → branch `claude/issue-27`, PR `Closes #27`. Pure refactor, no gameplay/visual change (island art is byte-for-byte the same SVGs, just sourced from data):
  - Exported **`YearId`** in `curriculum/types.ts` as the single source of truth; every other union now derives from it (`storage.ts` SaveData.year, `curriculum/index.ts` topicsFor, `game/memory.ts`, `game/parents.ts`). The literal `'reception' | 'year1' | 'year2'` now appears exactly once in `src/`.
  - Moved per-year assets onto **`YearInfo`**: `blurb` (was `ISLAND_BLURB` in home.ts), `art` (the island SVG data URI, was `.i0/.i1/.i2` CSS rules), `tint` (border colour, was `.iN.island`), and `maxAnswer` (curriculum range ceiling: R 30 / Y1 120 / Y2 130 — measured maxima 28/110/120 with headroom).
  - `home.ts` renders island art/tint/hover-stagger **inline from data**; `style.css` drops the per-index rules and drives the grid from `--cols:${YEARS.length}` and the border from `--tint` (with `.island.sel` still winning). `THEMES` is now `Partial<Record<YearId, Theme[]>>` with a Reception fallback in `pickTheme`, so a new year without decks still works. `data-subject` added to topic buttons.
  - Curriculum range guard now reads `year.maxAnswer` instead of a hard `<= 120`, so Y3+ answers won't be rejected. The e2e `startTopic` helper picks the subject tab from the registry (`TOPICS`) instead of a brittle topic-id regex.
  - **Guard rails**: new unit rail "the year union is defined once (YearId)" (counts the literal, expects only types.ts); new e2e rail "island art and grid come from year data" (each island has a distinct `background-image`, `.islands` carries `--cols`).
- Tests: `npx tsc --noEmit` clean, unit **329/329** (guardrails 9, incl. the new rail), build ok. e2e ran green in this cloud session (Chromium is pre-installed here) and — the authoritative gate — **CI on #78 is green** (run #8, full mobile+desktop incl. the two new rails).
- **main was red / #77:** the first CI run on #78 failed all 70 e2e with `Failed to launch chromium … /opt/pw-browsers/chromium` — a pre-existing bug in `playwright.config.ts` (dev-container path pinned on any Linux, so a GitHub runner's `playwright install` was overridden). Not this PR's diff; **main itself was red** (run #2). #77 fixes it (bundled binary only when it exists) and was **merged to main** by the owner (`2fb8fd5`) during this run. Merged the fixed main into #78 (`8f439ff`, clean auto-merge) → CI green.
- **Concurrent-run collision (#27):** a parallel routine run opened **#76** ("#27 part 1 — type-only", does *not* close #27) at 00:43, ~7 min before my #78; my initial open-PR scan didn't show it (a race). #78 is a superset that closes #27. Both are green; they shouldn't both merge. Flagged on both PRs + notified the owner to pick one (merge #78 and close #76, or merge #76 and I rebase #78 to just the enablers). I did **not** merge my own PR or touch #76.
- Owner action open: **#27** pick #76 vs #78; **#48** projectile art + TNT fix (PR #60); **#9** try the grown-ups dashboard; **#5** Master Ninja visuals; republish the artifact to ship the merged #8/#9 work.
- Next run: review/QA the chosen #27 PR (a *different* agent). Freeze still holds — next code-health item after #27 is **#26** (game-mode ternary chains). #60 still awaits its developer + owner.

## 2026-09-07 01:40 (interactive session, Opus)
- Code-health freeze (owner): no new feature work while any `review`/`debt` issue is open. Same wording in `CLAUDE.md`, `BACKLOG.md`, `docs/ROUTINE-PROMPT.md`; issue #46 rebuilt into "🔧 Code health" (21 items, guard rails first) and "🧊 Features — frozen".
- Guard rails (#73, PR #74 → #77 → #79): `tests/unit/guardrails.test.ts` (budgets for `as any` ≤2, comparator shuffles ≤4, `shadowBlur` ≤5; dependency allowlist; `window.__sna` hooks; listener pairing; router teardown; an anti-vacuity check) and three `guard rail:` e2e tests (render frame rate, screen teardown on a route change, screen-class CSS collisions). Every rail proven red on the bug it names.
- `.github/workflows/ci.yml`: tsc + unit + build + both e2e projects on every PR and push to main. Until this existed the rails ran nowhere on a PR.
- Fixed along the way: `Arena.destroy()` listener leak; the back button left a whole play screen running (rAF loop on a detached canvas, window listeners attached, `window.__sna` pointing at the dead session) — `playScreen`/`memoryScreen` return their `cleanup` and the router disposes the screen it replaces; the frame-rate rail measured `arena.time`, which is clamped at 0.1 s/frame and read 1.00x while the phone ran at 20 fps, so it counts frames now (clean 60 fps both projects; 19.9 / 4.3 with the `drift` bug back, floor 30).
- Process: #74 was merged by a routine run while its second review requested changes and its CI was red, leaving main red for ~20 minutes. #77 fixed it (`playwright.config.ts` pinned Chromium to the dev container's path, so CI could never launch a browser). Two rules to add to the routine prompt: never merge a PR whose latest review requests changes, and never merge one whose CI is red.
- Three review rounds, each by a different agent. Tests on main: tsc clean, 328 unit, 68 e2e (mobile + desktop), CI green on GitHub.
- Open: PR #79 (router rail slice bound — a broken last route could pass), #78 and #76 both implement #27 (duplicate work by two routine runs — one should be closed), #60 (ninja star) has requested changes.

## 2026-09-07 01:55 (hourly routine, cloud)
- Review/QA (reviewer role, a *different* agent from the developers). Cleared the open-PR queue:
  - **PR #79** (router rail slice bound, by the interactive Opus session): correct — the guard-rail slice is now bounded at the router's closing `};` and asserts both ends were found, so a broken last route (`parents`) can no longer pass by borrowing a `leave()` lower in the file. CI green on `b9f9a3c`. Squash-merged as `ccf31e9`.
  - **PR #78** (#27 — single-source `YearId` + per-year assets on `YearInfo`): read the diff against #27's acceptance criteria + CLAUDE.md — clean additive refactor, island SVGs byte-identical, range guard now per-year (`maxAnswer`, which *tightens* Reception 120→30), new unit + e2e rails. Resolved a WORKLOG merge conflict against main (`dc924be`), re-validated: tsc clean, 329 unit, build ok, and CI green on the merged head (run #13, full mobile+desktop e2e). Eyeballed the map at 390 px — islands render identically. Squash-merged as `83f4870`; **#27 closed**.
  - **PR #76** (#27 part 1 — type-only, a concurrent run's subset of #78): closed as superseded — #78 is a strict superset that landed. Nothing lost.
  - **PR #60** (#48 ninja star): standing CHANGES REQUESTED (TNT double-explode) + owner projectile-art approval, no new commits — nothing to re-review.
  - **PR #80** (interactive session: routine prompt → `docs/ROUTINE-PROMPT.md`, freeze becomes a `frozen` label, P1/P2/P3 reorg): left for the owner — it rewrites the routine's own operating rules, not a routine's call to self-merge.
- Developed **#26 review/debt — adding a game mode meant editing ~12 ternary chains in 5 files** → branch `claude/jolly-gauss-o0u9fx` (this run's designated push branch), PR (does **not** `Close #26` — a first slice, see below):
  - New pure **`src/game/modes.ts`**: a `MODES` table (one `ModeSpec` per mode) holding each mode's behaviour — `difficulty`, `speed`, `basePoints`, end `stars`, `coins`, and the `hasLives`/`staged`/`timed`/`boss`/`villain` flags + result-screen labels. `Mode` now lives here (re-exported from `session.ts` for existing imports).
  - **`session.ts`** reads the table via `this.spec` — every `o.mode === '…'` ternary (difficulty/speed/points/stars/coins/lives/staging/boss) is gone from the core loop; `session.test.ts` stays green, proving the scoring is byte-identical.
  - **`play.ts`** derives `title`/`villainMode`/`sprint`/`boss` and the results heading from the table too.
  - **Guard rail** (`guardrails.test.ts`): "session behaviour comes from the MODES table, not mode-literal ternaries" — proven red by re-introducing a `o.mode === 'boss'` comparison, green once removed. New `modes.test.ts` (7) covers the table directly.
  - **Deliberately deferred** (noted in the PR, so #26 stays open): `home.ts` island mode buttons (they mix non-`Mode` flows — Sensei training, Memory Match) and the **storage `bests` consolidation** (`endless`/`sprint`/`boss`/… → one `Record<Mode, …>`), which would change the save format and should ship with the #38 save-migration rather than break existing saves. Also the cosmetic medal/headline ternaries in play.ts.
- Tests: `tsc` clean, unit **337/337** (+7 modes, +1 rail), build ok, e2e mobile green (cloud). Full mobile+desktop e2e is the reviewer's QA / CI.
- Owner action open: **#48** projectile art + TNT fix (PR #60); **#80** review/land the routine-prompt + freeze-label change; **#5** Master Ninja visuals; republish the artifact to ship the merged #8/#9/#27 work.
- Next run: review/QA this #26 PR (a *different* agent: full mobile+desktop e2e + read the diff). Freeze still holds — next code-health item after #26 is **#45** (mode naming collision + `.storm` class). #60 still awaits its developer + owner.

## 2026-09-07 02:55 (hourly routine, cloud)
- Review/QA (reviewer role, a *different* agent from the developers):
  - **PR #81** (#26 — mode behaviour in one `MODES`/`ModeSpec` table, by session 019MhT6A…): read the diff against #26 + CLAUDE.md and verified the refactor is byte-identical per mode (difficulty/speed/basePoints, the four stars formulas, the composite coins formula, and every results-heading branch all reproduce the old code; `session.test.ts` unchanged proves the scoring). Guard rail (no `o.mode === '<literal>'` in session.ts) + `modes.test.ts` sound. Tests on `d5b1deb`: tsc clean, unit **337/337**, build ok, **mobile e2e 35/35** locally (12.3m) + CI run #16 green (full mobile+desktop). Posted QA-PASS (COMMENT — shared token can't self-approve) and **squash-merged as `caf5b89`**. #26 **kept open** for its two deferred follow-ups (home.ts buttons from the table; storage `bests` consolidation with #38's save-migration); Review/QA ticked; #46 unchanged for #26 (still open), **#27 ticked** (it closed earlier).
  - **PR #80** (routine-prompt-from-repo + `frozen` label): **left for the owner** — it rewrites the routine's own operating rules (never-merge-red/changes-requested, freeze-as-label, the bootstrap the owner must paste), which is the owner's governance call, not a routine's to self-merge. Consistent with the 01:55 run's decision. CI green; carries a stray `scripts/issues-created.json` worth dropping.
  - **PR #60** (#48 ninja star): standing CHANGES REQUESTED (TNT double-explode) + owner projectile-art approval, no new commits — nothing to re-review.
- Developed **#45 review/debt — `mode` naming collision (Session.Mode vs Topic.mode) + `.storm` class on every mode button** → branch `claude/jolly-gauss-62ub16` (this run's designated push branch), PR `Closes #45`. Pure rename, no gameplay or visual change:
  - `Topic.mode` → **`Topic.input: 'bubbles' | 'tracing'`** across `curriculum/types.ts` and every reader (writing trace topics, `home.ts`, `play.ts`, `sensei.ts`) + the unit tests that read it (`session.test.ts`, `sensei.test.ts`, `curriculum.test.ts`). The word `mode` now means only the play mode (`Session.Mode`); a topic's answer style is `input`.
  - `.storm` → **`.mode-btn`** on the island menu buttons (`home.ts` + `style.css`, modifiers `.train`/`.sprint`/`.boss`/`.memory` kept), and the e2e "mode cards match" selector updated.
  - **Guard rail** (`guardrails.test.ts`): "a topic answers via `input`, and menu buttons use `.mode-btn` not `.storm`" — no `Topic.mode` field and no `.storm` class in the TS sources. Proven red by re-adding `class="btn storm train"`, green once reverted. (CSS text can't be read by vitest — the `.storm`→`.mode-btn` CSS and the layout stay covered by the existing e2e "mode cards match" rail.)
- Tests: `tsc --noEmit` clean, unit **338/338** (+1 rail), build ok, e2e mobile green (cloud). Full mobile+desktop e2e is the reviewer's QA / CI.
- Owner action open: **#80** review/land the routine-prompt + freeze-label change (needs the one-time bootstrap paste); **#48** projectile art + TNT fix (PR #60); **#5** Master Ninja visuals; republish the artifact to ship the merged #8/#9/#26/#27 work.
- Next run: review/QA this #45 PR (a *different* agent: full mobile+desktop e2e + read the diff). Freeze still holds — next code-health item after #45 is **#34** (window.__sna untyped). #60 still awaits its developer + owner.

## 2026-09-07 03:xx (hourly routine, cloud)
- **Review/QA (reviewer role, a *different* agent from the developer).**
  - **PR #82** (#45 — `mode` naming collision + `.storm` class): read the diff against #45's acceptance criteria + CLAUDE.md. Clean, focused rename — `Topic.mode` → `Topic.input: 'bubbles' | 'tracing'` across `types.ts` + every reader; `.storm` → `.mode-btn` on the island buttons (modifiers unchanged); new guard rail (no `Topic.mode`, no bare `.storm`). Grepped the tree: no `Topic.mode` field, no `.storm` class in markup/CSS remain (the surviving "storm" hits are the *Sky Storm* mode name, the `storm80` dojo challenge, a spelling word). Tests: tsc clean, unit **338/338**, build ok, **e2e 70/70 mobile+desktop green** (14.6m), CI green on `5cdc3426`. **Squash-merged as `fc0f128`; #45 closed.**
  - **PR #60** (#48 ninja star): standing CHANGES REQUESTED (TNT double-explode) + owner projectile-art approval, no new commits since 09-06 — nothing to re-review.
  - **PR #80** (routine-prompt → repo + freeze becomes a label): left for the owner — it rewrites the routine's own operating rules + needs a one-time bootstrap paste, not a routine's call to self-merge.
- **Developed the second slice of #26 (the `home.ts` follow-up)** → branch `claude/jolly-gauss-5ov6m8`, **PR #83** (does **not** `Close #26`):
  - The five island menu buttons were five hardcoded `<button class="btn mode-btn …">` lines + five separate click handlers; they now render from **one `menu` table** in `islandScreen` and are wired in a single loop. The three battle modes take their **title from `MODES`** (Sky Storm / Ninja Sprint / Boss Battle); Sensei-training and Memory-Match are non-`Mode` flows that share the same home.ts table (per the acceptance-criteria note). Pure refactor — markup byte-identical, no visual/behaviour change.
  - **Guard rail** (`guardrails.test.ts`): the island buttons render from one table — `class="btn mode-btn` appears exactly once. Proven red on a re-added hardcoded button.
  - Tests: tsc clean, unit **339/339** (+1 rail), build ok, **e2e 70/70 mobile+desktop green** (cloud). 
  - **Deferred (why #83 doesn't close #26):** per-mode `bests` → one `Record<Mode, …>` changes the save format, so it ships **with #38's** save-migration. #26 stays open for it.
- Owner action open: **#80** review/land routine-prompt + freeze-label (one-time bootstrap paste); **#48** projectile art + TNT fix (PR #60); **#5** Master Ninja visuals; republish the artifact to ship the merged #8/#9/#26/#27/#45 work.
- Next run: review/QA **PR #83** (a *different* agent: full mobile+desktop e2e + read the diff). Freeze still holds — after #26's last item lands with #38, next code-health item is **#34** (window.__sna untyped).

## 2026-09-07 04:xx (hourly routine, cloud)
- **Review/QA (reviewer role, a *different* agent from the developer).**
  - **PR #83** (#26 — island menu buttons from one table, by session 01VF7aEz…): read the diff against #26's acceptance criteria + CLAUDE.md. Pure refactor — the five island buttons render from one `menu` table + a single click-handler loop; markup verified byte-identical (same classes incl. the `train`/`sprint`/`boss`/`memory` modifiers, ids, `.vport`, titles, blurbs, order); battle-mode titles single-sourced from `MODES` (checked against `src/game/modes.ts`). New guard rail sound. Tests on `fe274e6`: tsc clean, unit **339/339**, build ok, **e2e 70/70 mobile+desktop green** (19.0m), CI check "test" green (confirmed via REST — the GitHub MCP `get_status` is blind to Actions check-runs). **Squash-merged as `552a6b5`.** #26 kept open for its one deferred item (storage `bests` → one `Record<Mode, …>`, ships with #38's save-migration); ticked the home.ts item + Review/QA in #26. Also ticked **#45** in #46 (merged `fc0f128` last run but left unticked).
  - **PR #80** (routine-prompt → repo + freeze becomes a label): left for the owner — rewrites the routine's own operating rules + needs a one-time bootstrap paste, not a routine's call to self-merge.
  - **PR #60** (#48 ninja star): standing CHANGES REQUESTED + owner projectile-art approval, no new commits — nothing to re-review.
- **Developed #34 review/debt — `window.__sna` untyped (`any`)** → branch `claude/jolly-gauss-ta6roa` (this run's designated push branch), PR `Closes #34`. Pure typing change, no runtime/gameplay change:
  - New **`src/ui/hooks.ts`**: `PlayHooks` / `MemoryHooks` interfaces + the view/state types the e2e spec reads (`BubbleView`, `PlayState`, `MemoryCardView`, `MemoryState`), and a global `interface Window { __sna?: SnaHooks }`.
  - `play.ts` / `memory.ts` build the hook object as a typed `const hooks: PlayHooks|MemoryHooks = {…}; window.__sna = hooks;` — no more `(window as any).__sna` (and `delete window.__sna` on cleanup).
  - `tests/e2e/game.spec.ts` replaces `interface Window { __sna: any }` with `import type { PlayHooks, MemoryHooks }` and views the global as their intersection (tests/e2e is outside tsconfig's `include`, so it never clashes with hooks.ts's declaration).
  - **Guard rail** (`guardrails.test.ts`): "the __sna hooks are typed via hooks.ts, not `any`" — neither screen casts the global through `any`, and hooks.ts declares both interfaces. **Proven red** by re-adding `(window as any).__sna`, green once reverted. Also demonstrated the real win: renaming a hook now fails **tsc** (TS2561) instead of only at runtime.
- Tests: `tsc --noEmit` clean, unit **340/340** (+1 rail), build ok, **e2e mobile 35/35 green** (cloud, 13.8m). Full mobile+desktop e2e is the reviewer's QA / CI.
- Owner action open: **#80** review/land routine-prompt + freeze-label (one-time bootstrap paste); **#48** projectile art + TNT fix (PR #60); **#5** Master Ninja visuals; republish the artifact to ship the merged #8/#9/#26/#27/#45 work.
- Next run: review/QA this **#34 PR** (a *different* agent: full mobile+desktop e2e + read the diff). Freeze still holds — next code-health item after #34 is **#33** (`as any` physics: `_g` gravity, Tracer `_size`). #60 still awaits its developer + owner.

## 2026-09-07 05:xx (hourly routine, cloud)
- **Review/QA (reviewer role, a *different* agent from the developer).**
  - **PR #84** (#34 — `window.__sna` typed via `src/ui/hooks.ts`, by session 01HFSNAb…): read the diff against #34's acceptance criteria + CLAUDE.md. Clean pure-typing change — new `hooks.ts` declares `PlayHooks`/`MemoryHooks` (+ the view/state types the spec reads) and `Window.__sna?: SnaHooks`; `play.ts`/`memory.ts` build a typed `const hooks` and `delete window.__sna` on cleanup; the e2e spec drops `__sna: any` for `import type`. Grepped: no `(window as any).__sna` in `src/` (only the unrelated `webkitAudioContext` fallback + comments). Guard rail sound. Tests on `2170c1e`: tsc clean, unit **340/340**, build ok (this reviewer), **CI green on the head SHA** (run 34086428071: tsc + unit + build + mobile & desktop e2e). Posted QA-PASS (COMMENT — shared token can't self-approve) and **squash-merged as `ac6429f`; #34 closed.**
  - **PR #80** (routine-prompt → repo + freeze becomes a label): left for the owner — rewrites the routine's own operating rules + needs a one-time bootstrap paste, not a routine's call to self-merge.
  - **PR #60** (#48 ninja star): standing CHANGES REQUESTED + owner projectile-art approval, no new commits — nothing to re-review.
- **Developed #33 review/debt — physics smuggled through `as any`** → branch `claude/jolly-gauss-72v75j` (this run's designated push branch), **PR #85** `Closes #33`. Pure typing/mechanical, no behaviour change:
  - `Bubble` gains a real **`g: number`** field. Spawn sets `g` inside the object literal (was `(this.bubbles[…] as any)._g = g` on the next line); the outcome-reveal bubble sets `g: this.g` — byte-identical to the old `?? this.g` fallback (600). `update()` reads `b.g` directly. The e2e freeze helper sets `x.g` instead of the untyped `x._g`.
  - `Tracer._size` named in the issue **no longer exists in the tree** (removed by an earlier refactor) — nothing to delete.
  - **Guard rail**: the `/src/game/` `as any` budget **drops 2 → 0** (game logic now carries no `as any`).
- Tests: `tsc --noEmit` clean, unit **340/340** (budget-0 rail green), build ok, **e2e mobile 35/35 green** (cloud, 12.9m). Full mobile+desktop e2e is the reviewer's QA / CI.
- Owner action open: **#80** review/land routine-prompt + freeze-label (one-time bootstrap paste); **#48** projectile art + TNT fix (PR #60); **#5** Master Ninja visuals; republish the artifact to ship the merged #8/#9/#26/#27/#34/#45 work.
- Next run: review/QA this **#33 PR #85** (a *different* agent: full mobile+desktop e2e + read the diff). Freeze still holds — next code-health item after #33 is **#37** (dead code: Arena.flash/time, Bubble.scale, dom.wait, totalSlices, yearById). #60 still awaits its developer + owner.

## 2026-09-07 06:xx (hourly routine, cloud)
- **Review/QA (reviewer role, a *different* agent from the developer).**
  - **PR #85** (#33 — `Bubble.g` a real field, not `as any`, by `session_01M1Ueff…`): read the diff against #33's acceptance criteria + CLAUDE.md. Clean pure-typing change — `Bubble` gains `g: number`, both push sites set it in the object literal (`g: this.g` on the outcome-reveal bubble is byte-identical to the old `?? this.g` fallback), `update()` reads `b.g`, the e2e freeze helper sets the typed `x.g`; `/src/game/` `as any` budget lowered **2 → 0**; `Tracer._size` confirmed already gone. Tests on `5e3df9e`: tsc clean, unit **340/340**, build ok (this reviewer); **CI run `34088731008` green on the head SHA** (tsc + unit + build + mobile & desktop e2e). Posted QA-PASS (COMMENT — shared token can't self-approve) and **squash-merged as `b8194b9`; #33 closed.** Ticked #33 in #46.
  - **PR #80** (routine-prompt → repo + freeze label): still owner-only (rewrites the routine's own rules + one-time bootstrap paste) — left for the owner.
  - **PR #60** (#48 ninja star): standing CHANGES REQUESTED + owner projectile-art approval, no new commits — nothing to re-review.
- **Developed #37 review/debt — dead code** → branch `claude/jolly-gauss-4c8qmz` (this run's designated push branch), **PR #87** `Closes #37`. Pure deletion, no runtime/gameplay/visual change:
  - Removed **`Bubble.scale`** (always 1 — interface + both literals; `drawBubble` starts from a literal `1`), **`SaveData.totalSlices`** (written in DEFAULT, never read — type + default + `parents.test` fixture; no migration, old saves keep an ignored key), **`dom.wait`** and **`curriculum.yearById`** (exported, imported nowhere; the now-unused `YEARS` import went with `yearById`).
  - **Kept, contrary to the issue's list:** `Arena.time` is **live** — two e2e guard rails read it (frame-rate + dead-arena teardown); `Arena.flash()` was already absent. Flagged both on the issue before deleting.
  - **Guard rail** (`guardrails.test.ts`): "dead symbols removed in #37 stay gone" — greps the four back out (tsc's `noUnusedLocals` misses unused *exports* / always-constant fields). **Proven red** by re-adding `yearById`.
  - Tests: `tsc --noEmit` clean, unit **341/341** (+1 rail), build ok (bundle 146.41 → 146.37 kB), **e2e mobile 35/35 green** (cloud, 12.6m — includes the two `Arena.time` rails). Full mobile+desktop e2e is the reviewer's QA / CI.
- Owner action open: **#80** review/land routine-prompt + freeze-label (one-time bootstrap paste); **#48** projectile art + TNT fix (PR #60); **#5** Master Ninja visuals; republish the artifact to ship the merged #8/#9/#26/#27/#33/#34/#45 work.
- Next run: review/QA this **#37 PR #87** (a *different* agent: full mobile+desktop e2e + read the diff). Freeze still holds — next code-health item after #37 is **#42** (biased comparator shuffles; there is one in `arena.ts:90` `.sort(() => Math.random() - 0.5)`). #26's last item is still parked on #38's save-migration; #60 still awaits its developer + owner.

## 2026-09-07 07:xx (hourly routine, cloud)
- **Review/QA (reviewer role, a *different* agent from the developer).**
  - **PR #87** (#37 — dead code: Bubble.scale, dom.wait, totalSlices, yearById): read the diff against #37's acceptance criteria + CLAUDE.md. Clean pure-deletion change — removed `yearById` (+ its now-unused `YEARS` import), `Bubble.scale` (was always 1; `drawBubble` starts from a literal 1), `SaveData.totalSlices` (written, never read; no migration, old saves keep an ignored key), `dom.wait` (exported, imported nowhere); kept `Arena.time` (live — two e2e rails read it) and noted `Arena.flash` was already gone; new "dead symbols removed in #37 stay gone" guard rail. Grepped the tree: no lingering references (the surviving `.scales`/`c.scale()`/`.storm` hits are unrelated). Tests on `8b60d7a`: tsc clean, unit **341/341**, build ok (146.37 kB), **e2e 70/70 mobile+desktop green** (15.9m, this reviewer); CI "test" check green on the head SHA (run 34093167491). Posted QA-PASS (COMMENT — shared token can't self-approve) and **squash-merged as `37c49ab`; #37 closed.** Ticked #37 in #46.
  - **PR #86** (routine-prompt nits → ROUTINE-PROMPT.md/CLAUDE.md/BACKLOG.md) and **PR #88** (CI: full suite once per change): #88 was **merged into main during this run** (`4c62a0b`, by the owner or a concurrent run) — CI now runs the full mobile+desktop e2e on PRs + nightly, and only the light run (tsc+unit+build) on push to main. #86 (its matching routine-doc rewrite) is still open and is owner-governance (it rewrites the routine's own operating rules), so left for the owner; note the temporary doc/config gap between the merged #88 and the unmerged #86. Also observed #89 (`7e229a0`) landed on main during the run. **PR #60** (#48 ninja star): standing CHANGES REQUESTED + owner projectile-art approval, no new commits — nothing to re-review.
- **Developed #42 review/debt — biased comparator shuffles** → branch `claude/youthful-keller-xwy7wq` (this run's designated push branch), **PR #91** `Closes #42`. Pure fix, behaviour-preserving apart from the distribution:
  - All four `.sort(() => rng() - 0.5)` sites replaced with the uniform Fisher–Yates `shuffle(rng, arr)` from `curriculum/util`: `session.ts` `labelsFor` (seeded `this.rng`), and `arena.ts` ×3 (wave launch order, `dealOrdered` decoys pool, in-batch position — `Math.random`, a valid `Rng`). Each returns a permutation of the same elements; `dealOrdered` still consumes ordered targets in order across batches, so sequence questions still launch sliceable-in-order.
  - **Guard rail**: `no comparator shuffles` budget lowered **4 → 0** (the rail counted exactly the four before; none now).
- Tests: `tsc --noEmit` clean, unit **341/341** (budget-0 rail green), build ok (146.32 kB), **e2e mobile 35/35 green** (cloud, 12.9m). Full mobile+desktop e2e is the reviewer's QA / CI.
- Owner action open: **#86** review/land the routine-prompt nits (owner-governance); **#48** projectile art + TNT fix (PR #60); **#5** Master Ninja visuals; **#80** routine-prompt-to-repo + freeze-label (if still relevant after #86); republish the artifact to ship the merged #8/#9/#26/#27/#33/#34/#37/#45 work.
- Next run: review/QA this **#42 PR #91** (a *different* agent: full mobile+desktop e2e + read the diff). Freeze still holds — next code-health item after #42 is **#39** (Tracer leaks canvas listeners per question). #26's last item is still parked on #38's save-migration; #60 still awaits its developer + owner.

## 2026-09-07 08:xx (hourly routine, cloud)
- **Review/QA (reviewer role, a *different* agent from the developer).**
  - **PR #91** (#42 — biased comparator shuffles → Fisher–Yates, by `session_01GK528…`): read the diff against #42's acceptance criteria + CLAUDE.md. Clean, behaviour-preserving change — all four `.sort(() => rng() - 0.5)` sites replaced with the non-mutating `shuffle(rng, arr)` from `curriculum/util` (`session.ts labelsFor` on the seeded `this.rng`; `arena.ts` ×3 on `Math.random`, a valid `Rng`); each returns a permutation of the same elements, only the distribution changes (now uniform); verified `dealOrdered` still consumes ordered targets in order across batches (only decoy pool + in-batch positions shuffled). Guard-rail `no comparator shuffles` budget **4 → 0** (correct — counted exactly those four). Tests on `a865229`: tsc clean, unit **341/341**, build ok (146.32 kB), **e2e 70/70 mobile+desktop green** (15.0m, this reviewer), CI green on head SHA. Posted QA-PASS (COMMENT — shared token can't self-approve) and **squash-merged as `a305799`; #42 closed.** Ticked #42 in #46 + its workflow boxes.
  - **PR #90** (routine-prompt: restate four rules at their decision point) — owner-governance (rewrites the routine's own operating prompt), left for the owner. **PR #60** (#48 ninja star): standing CHANGES REQUESTED + owner projectile-art approval, no new commits — nothing to re-review.
- **Developed #39 review/debt — Tracer leaks canvas listeners per question** → branch `claude/youthful-keller-z3zqmi` (this run's designated push branch), **PR #92** `Closes #39`. **No `src/` change:** the leak was already closed in code (`adf3a39`, ~11 min after the issue was filed — `Tracer.destroy()` unbinds the `#trace` canvas `pointerdown`/`pointermove` listeners; `play.ts startTrace` disposes the previous Tracer before building a new one per question; `cleanup()` disposes on teardown). What was missing was the regression guard for the **second** failure mode: the listener-pairing rail locks half (destroy unbinds what the ctor binds), but nothing stopped a new Tracer being built per question without disposing the old one.
  - **Guard rail** (`guardrails.test.ts`): "play.ts disposes the previous Tracer before building a new one (#39)" — one `new Tracer(` site, inside `startTrace` behind a `tracer?.destroy()`, and `cleanup()` disposes it. **Proven red** by removing the dispose-before-construct call.
- Tests: `tsc --noEmit` clean, unit **342/342** (+1 rail), build ok (146.32 kB). Test-only change (no `src/` touched) → e2e unaffected; full mobile+desktop e2e is the reviewer's QA / CI.
- Owner action open: **#90** review/land routine-prompt restatement (owner-governance); **#48** projectile art + TNT fix (PR #60); **#5** Master Ninja visuals; republish the artifact to ship the merged #8/#9/#26/#27/#33/#34/#37/#42/#45 work.
- Next run: review/QA this **#39 PR #92** (a *different* agent). Freeze still holds — next code-health item after #39 is **#38** (save format has a version field but no migration; #26's last item — per-mode `bests` → one `Record<Mode, …>` — is parked to ship with it). #60 still awaits its developer + owner.

## 2026-09-07 09:xx (hourly routine, cloud)
- **Review/QA (reviewer role, a *different* agent from the developer).**
  - **PR #92** (#39 — guard the second half of the Tracer listener leak, by `session_01BHtoJ6…`): read the diff against #39 + CLAUDE.md. Test-only change — a single new guard rail *"play.ts disposes the previous Tracer before building a new one (#39)"*. Verified the underlying leak is genuinely already fixed in code: `tracing.ts:26` `destroy()` unbinds **both** `#trace` canvas listeners (`pointerdown`/`pointermove`) and the window listeners; `play.ts:146-148` has a single `new Tracer(` site inside `startTrace` behind `tracer?.destroy()`, and `cleanup()` disposes on teardown. Rail is real, not vacuous — **proven red** by removing the dispose-before-construct call (`disposesFirst` → false). Tests on `7114344`: tsc clean, unit **342/342**, build ok, **e2e 70/70 mobile+desktop green** (15.5m, this reviewer), CI run `34103406717` green on head SHA. Posted QA-PASS (COMMENT — shared token can't self-approve) and **squash-merged as `313ed9d`; #39 closed.** Ticked #39 in #46.
  - **PR #90** (routine-prompt: restate four rules at their decision point) — owner-governance (rewrites the routine's own operating prompt), left for the owner. **PR #60** (#48 ninja star): standing CHANGES REQUESTED + owner projectile-art approval, no new commits — nothing to re-review.
  - Main is green (push run #39 `a305799` success). The #88 nightly full-suite (03:20 UTC) first fires tomorrow — nothing to read yet.
- **Developed #38 review/debt — save format has a version field but no migration** → branch `claude/youthful-keller-ylbozp`, **PR #93** `Closes #38`. Behaviour-preserving for current saves:
  - Added **`SAVE_VERSION`** (1) + **`migrate(raw)`**: drives off `raw.v` (not the key name), walks a `MIGRATIONS` map of per-version steps, fills missing keys from `DEFAULT`, stamps the current version; tolerant of corrupt/array/non-object data → fresh default; a `v`-less blob is treated as v1. `load()` now routes the stored blob through `migrate()`. `MIGRATIONS` is **empty on v1** (commented example shows #26's `bests` fold slotting in as step 1), so for any current v1 save the result is byte-identical to the old `{ ...DEFAULT, ...JSON.parse }` shallow-merge — no behaviour change, no save touched.
  - Used a `MIGRATIONS` map + version walk rather than a literal `switch (raw.v)` (same behaviour, each step a named add-only function) — flagged for the reviewer in the PR.
  - **Guard rail**: "storage.load() routes stored data through migrate() (#38)" — asserts `load()` calls `migrate(`, the old shallow-merge is gone, and `migrate` keys off the version. **Proven red** by reverting `load()` to the shallow-merge.
- Tests: `tsc --noEmit` clean, unit **348/348** (+6: 5 migration cases + 1 rail), build ok (146.50 kB), **e2e mobile 35/35 green** (cloud, 12.7m). Full mobile+desktop e2e is the reviewer's QA / CI.
- Owner action open: **#90** review/land routine-prompt restatement (owner-governance); **#48** projectile art + TNT fix (PR #60); **#5** Master Ninja visuals; republish the artifact to ship the merged #8/#9/#26/#27/#33/#34/#37/#39/#42/#45 work.
- Next run: review/QA this **#38 PR #93** (a *different* agent: full mobile+desktop e2e + read the diff — note the `MIGRATIONS`-map vs literal-`switch` choice). Freeze still holds — next code-health item after #38 is **#35** (screen scaffolding duplicated between play.ts and memory.ts). #26's last item (per-mode `bests` → `Record<Mode, …>`) can now build on #38's migration seam as its first real step. #60 still awaits its developer + owner.

## 2026-09-07 10:xx (hourly routine, cloud)
- **Review/QA (reviewer role, a *different* agent from the developer).**
  - **PR #93** (#38 — save format has a version field but no migration, by `session_01VDgLDHaB7cTotMF2ixiftf`): read the diff against #38 + CLAUDE.md. Clean, minimal, behaviour-preserving change to `src/storage.ts` — `SAVE_VERSION` (1) + `migrate(raw)` drives off `raw.v` (not the key name), walks a `MIGRATIONS` map (empty on v1, so the `while` loop never runs), fills missing keys from `DEFAULT`, stamps the version; `load()` routes the stored blob through `migrate()`. Verified byte-identical to the old shallow-merge for any current v1 save; a `v`-less blob is treated as v1 and stamped (an improvement); corrupt/array/non-object → fresh default. Guard rail *"storage.load() routes stored data through migrate() (#38)"* is genuine (slices `load()`, requires `migrate(`, forbids the old `{ ...DEFAULT, ...JSON.parse` merge, requires `MIGRATIONS[`) — reverting to the shallow-merge turns it red. Tests on `6fb721e`: tsc clean, unit **348/348**, build ok (146.50 kB), **e2e 70/70 mobile+desktop green** (14.6m, this reviewer), CI "CI" check green on head SHA (run 34110277816). Posted QA-PASS (COMMENT — shared token can't self-approve) and **squash-merged as `b4871b5`; #38 closed.** Ticked #38 in #46.
  - **PR #90** (routine-prompt: restate four rules) — owner-governance (rewrites the routine's own operating prompt), left for the owner. **PR #60** (#48 ninja star): standing CHANGES REQUESTED + owner projectile-art approval, no new commits — nothing to re-review. **#26** was **closed by the owner** (07:58) as completed — its `#46` box is still unticked but the issue is done; its one deferred item (per-mode `bests`) was dropped, so I did not develop it. Nightly full-suite (03:20 UTC, from #88) has not fired its first run yet — nothing to read. Main green on push (`b4871b5`).
- **Developed #35 review/debt — screen scaffolding duplicated between play.ts and memory.ts** → branch `claude/affectionate-noether-1czs8u`, **PR #94** `Part of #35` (two riskier items deferred; see below). Behaviour-preserving apart from one disclosed cosmetic delta:
  - New **`src/ui/screen.ts` `screenScope()`**: owns the `alive`-guarded `later()`/timers, the `#toast` helper and `dispose()` (stop timers, cancel speech, drop `window.__sna`); `play.ts` + `memory.ts` both build on it, neither declares its own `later`/`toast`. **`stickersHTML(fresh)`** single-sources the "New sticker!" unlock markup. **`esc` now defined once** (dom.ts; `visuals.ts` imports it). **`coinLabel(p)`** in `curriculum/util.ts` replaces the `£/p` ternary copied in `visuals.ts`, `game/memory.ts`, `maths.ts`.
  - Cosmetic delta: Memory Match's generic toast now auto-hides after the shared 1300 ms default (was 1200 ms) — not gameplay, not asserted by any test.
  - **Guard rails** (`guardrails.test.ts`): *"esc defined once, in dom.ts (#35)"* and *"play + memory build on screenScope() (#35)"* — **both proven red** on their exact reversion (mutation-tested). Plus a `coinLabel` unit test.
  - **Deferred (still open on #35, so `Part of` not `Closes`):** `resultsModal()` extraction (the two modals differ substantially) and `SHAPES2D/3D` consolidation (different arities + curriculum-test risk).
- Tests: `tsc --noEmit` clean, unit **351/351** (+1 coinLabel, +2 rails), build ok (**145.98 kB**, down from 146.50 — the dedup), **e2e mobile 35/35 green** (cloud, 12.4m). Full mobile+desktop e2e is the reviewer's QA / CI.
- Owner action open: **#90** review/land routine-prompt restatement (owner-governance); **#48** projectile art + TNT fix (PR #60); **#5** Master Ninja visuals; republish the artifact to ship the merged #8/#9/#26/#27/#33/#34/#37/#38/#39/#42/#45 work.
- Next run: review/QA this **#35 PR #94** (a *different* agent: full mobile+desktop e2e + read the diff — note the 1200→1300 ms Memory toast delta and the two deferred follow-ups). Freeze still holds — next code-health item after #35 is **#36** (play.ts is one 225-line closure with long lines). #60 still awaits its developer + owner.

## 2026-09-07 15:40 (interactive session, Opus) — review gate live
- **Review gate (#95, merged `a1162b7`).** One GitHub token serves the owner and every agent, so every PR looks self-authored and the API refuses a formal CHANGES_REQUESTED review — the review rules were a convention nothing enforced, which is how #74 merged over five open review items. `.github/workflows/review-gate.yml` now stamps a `review-gate` commit status on the PR head: **red** while the PR is a draft or a `REVIEW: CHANGES REQUESTED` comment has no later `REVIEW: CLEARED`. "Never merge a PR whose CI is not green" already tops every merge, so the review rides on a rule that is obeyed.
- The predicate lives in `scripts/review-gate.mjs` with 18 cases in `tests/unit/review-gate.test.ts`, most of them text that must **not** be read as a marker. It is there because it failed *green* four times during review — publishing "no block" over a live one. The two markers are matched **asymmetrically on purpose**: the block loosely (bold, case, quoting forgiven — a false positive is red and self-correcting), the clear strictly (plain, capitals, at the very start — a false positive silently lifts a real block). Do not "tidy up the duplication".
- Verified in production, not by reading: block → clear → re-block cycles, five near-miss false clears that correctly did not clear, `synchronize` re-stamping a new head in seven seconds, and the `issue_comment` path stamping the PR head (ordinary comments skip the job and bill nothing).
- **Two things the next run will meet.** (1) **#60's head has no `review-gate` status** — it predates the workflow. That is exactly the case rule 1 now describes: a missing status has not been judged, so check the draft flag and `REVIEW:` comments by hand; never read absence as a pass. (2) `issue_comment` sub-types `edited`/`deleted` have no live exercise yet, and a block applied by comment alone (no draft) has only been tested through `pull_request` events — worth watching.
- **The nightly has never run.** `?branch=main&event=schedule` → `total_count: 0`; the cron landed at 07:54 today and 03:20 UTC has not come round. Since the push-to-main job skips e2e, no *merged* tree has yet had a full e2e check. STEP 1's nightly rule is being tightened (PR #96) so an empty list counts as red, not as "it did not fail".
- CI budget (#88, merged): PR runs stay full; push-to-main is types+unit+build; docs-only runs nothing; APK on tags/dispatch only. Measured effect on the merges since: shipping an issue costs ~15 min instead of ~42. Owner set the routine to 3-hourly. Repo stays private for now, so Actions minutes remain metered (2,000/month on Free).
