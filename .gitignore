node_modules
dist
test-results
playwright-report
.DS_Store
_to_delete
Claude outputs
_artifact_*.bundle

# Signing keys: never commit (the debug key lives in .android/, see .android/README.md)
*.keystore
*.jks

# InstructionsLoaded hook's debug log (#101 layer 4 verification) — per-machine, never a shared record
.claude/instructions-loaded.log

# The owner's-machine marker (#342). Never commit it: its whole job is to be absent in a cloned checkout,
# which is how .claude/hooks/write-guard.mjs tells an unattended run from a session with the owner.
.owner-machine

# #715: sketchbook screenshots are regenerated by `npm run sketch:shot`; the gallery artifact is the record, not the PNGs
docs/sketchbook/
