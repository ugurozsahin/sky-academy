# Worklog

**Nothing reads this file.** It is a write-only record: a run appends a short entry so a human can read back
what happened and why, and then never opens it again. It used to be the handoff between runs, from before the
backlog moved to GitHub Issues — by 2026-09-07 it had reached 95 KB and ~24,000 tokens, every run read all of
it at STEP 1, and a run finally died on the token limit doing so. An unbounded thing that every run must
consume is the same failure this project has hit in five other places.

Everything a run actually needs is somewhere bounded:

| what a run needs | where it is |
|---|---|
| what to work on next | issue #46, the owner's ordered list |
| what is already in flight | the open pull requests |
| why a change was made the way it was | that PR's description and its review comments |
| what happened when | `git log`, and the PRs it references |
| whether the routine is alive | the `routine: heartbeat` issue (the watchdog reads it) |
| whether the watchdog is alive | the `watchdog: heartbeat` issue (the routine reads it) |

Older entries are archived per month under `docs/worklog/`. Archive the current month and start a fresh file
whenever this one passes ~40 KB; nothing depends on its contents, so archiving can never break a run.

<!-- Entries below, newest at the bottom. Keep them short: PRs reviewed/merged, item developed + PR link,
     tests, guard-rail budgets changed. Anything a future run must act on belongs in an issue, not here. -->

## 2026-09-09 ~12:1x (reviewer session — a different agent from the ugurozsahin/sky-academy-private-archive#132 developer)

- **PR ugurozsahin/sky-academy-private-archive#132 merged to `main` (`3615850`, squash) — #48 tap-to-pop ninja star.** Re-application of the closed PR ugurozsahin/sky-academy-private-archive#60 on today's `main`, per the owner's 2026-09-09 decisions on #48. A single tap throws the avatar's own projectile from the bottom of the arena; it flies 150 ms (`SHOT_FLIGHT`) and pops the bubble on landing. Score, lives, the outcome reveal and the `__sna` hooks all settle **at the tap**, so the flight is decoration; a tapped bubble that falls off screen mid-flight is not a miss. Swipes are untouched. Ten per-element `SHOT_STYLE` projectiles (shuriken for Kai/Dusk/Master, fireball, orb, bolt, rock, leaf, shard, star, laser), element-particle wake, `sfx.whoosh()` on throw and the element slice + haptic on landing. `PlayState.shots` added to the `__sna` contract.
- **The ugurozsahin/sky-academy-private-archive#60 review's blocking TNT bug is designed out, not patched.** `hitBubble` consults `throwFor(b)` *before* `cb.onHit`; `play.ts` answers `false` for the `BOMB`, so a tapped 💣 never creates a `Shot` — one burst, immediately, `life` sound only. No double explosion, no slice sound rewarding the hit, no exploded bomb flying on. The bomb knowledge stays in `play.ts`; the arena stays generic. `clearWave()` also drops shots, and `landShot` bursts *and* sounds only when the bubble really pops.
- **Four review passes, three of them blocking** (reviewer ≠ developer, per the scrum rule). Worth recording because two of the three were invisible to `npm test` and `tsc`:
  1. The TNT e2e pinned "question 6" once, but a bomb never rides a sequence question and Sky Storm draws its topic at random — 2 of the 29 year-2 topics are sequences, so a ~7% per-project flake that failed *hard* (lives run out before the next bomb wave). Fixed with a retry that resets `questionsAsked` each attempt.
  2. The replacement wait returned `JSON.stringify({bomb:false})` — **truthy** — so it resolved on the first frame a bubble launched, before anything could be in range. Deterministic red. Fixed to boolean/`null` predicates reading `arena.bubbles` (which includes unlaunched bubbles, so a bomb riding the second batch is seen).
  3. The edit that fixed (2) ran past the end of the function and **deleted `solveCurrent`, `waitForWrongOrEnd` and `answerAll`** — 15 call sites across 10 of the 38 tests. `tsconfig.json` includes only `src` and `tests/unit`, so `tsc` never reads the e2e spec and Vitest never loads it: both local checks stayed green. **`npx playwright test --list` compiles every spec without launching a browser** and catches exactly this; it is now part of the pre-push checks. Worth considering as a CI step or a rail in its own right.
- **Owner-approval gate armed for the first time in anger.** The PR needed the owner's sign-off on the projectile art but carried only the area labels `art`/`playtest`; the `owner-approval` label did not exist in the repo. Created and applied, so `scripts/review-gate.mjs` held the merge red until he wrote `OWNER: APPROVED` (he did, himself, 12:08:12Z, after playing the preview build).
- **A relayed `OWNER: APPROVED` was blocked and withdrawn.** An agent posted the marker as a clearly-flagged relay of the owner's session message. CLAUDE.md's *"No agent ever writes those markers"* has no relay exception, and the reason is structural rather than about honesty: a flagged relay and a fabrication are the same bytes to every later reviewer, and the gate had *already* gone from three block reasons to two on the strength of it. The reviewer kept the block; the relay was edited to drop the marker; the owner posted his own. `review-gate.mjs:24-30` already names the durable fix — a second identity (machine account) so agents cannot produce his markers at all. **That deserves its own issue.**
- Tests on the merged head: CI green (unit + guard rails, build, **e2e mobile + desktop**). Mac: `playwright test --list` 74, `tsc --noEmit` clean, `npm test` **438/438**, build ok. **No guard-rail budget moved** — `shadowBlur` 0, `as any` 0, `drawBubble` still free of per-frame gradients/`measureText`; no dependency added.
- **PR ugurozsahin/sky-academy-private-archive#60 confirmed closed as superseded** (closed 10:33:12Z, never merged); its diff and review comment stay readable as the reference. Issue #48 closed by the merge, all six checklist boxes ticked.
- **Not done here, by instruction:** the artifact was **not** republished — the scheduled publisher task owns that, and it will pick this up from `main`.

## 2026-09-09 20:37Z — dev routine run
- **Reviewed + QA'd + merged PR ugurozsahin/sky-academy-private-archive#143** (`Part of #44`, font weights + cadence/publisher docs) → `9d78014`. Fourth agent on it: not the developer, not the reviewer who blocked it, not the run that cleared the block. Re-derived every load-bearing claim: Fredoka answers 200 for wght@500/600/700 and **400 for 800/900**; both new rails proven both ways (arena.ts, visuals.ts and style.css reverted to 800 → red; restored → 34/34); the three merge resolutions checked by comparing rail *titles* against main (32 → 34, none dropped) rather than by reading the diff; the docs commit checked against the live triggers (dev cron `37 */2 * * *`, watchdog `0 5,11,17,23 * * *`, publisher `enabled: false` since 11:12Z) — TIGHTENING confirmed. QA: tsc clean, unit 464/464, build 152.21 kB, **full mobile+desktop e2e 78/78 in 19.4 min**. #44 stays open: the woff2 half is the owner's call.
- **Developed #40** (speech `cancel()`+`speak()` on every call) → PR ugurozsahin/sky-academy-private-archive#149, branch `claude/bold-knuth-9054nm`. `say()` now cancels only when the engine reports speaking/pending and speaks on the next macrotask after a cancel; a superseded line is dropped. Per-letter progress speech takes a new `{ queue: true }` form rather than being silenced, so a pre-reader still hears the letters in order. Engine injectable, as navigator is for `haptic()`. 6 unit tests + 1 guard rail (`per-letter progress speech is queued, never interrupting (#40)`), proven both ways. tsc clean, unit 471/471 after merging main, build 152.42 kB. No budget moved, no dependency, no visual change.
- Checks: nightly on main green (08:08:46Z, `6d0332c`, within 26h) · watchdog pulse #61 at 17:05Z, fresh · freeze holding, #46 order untouched.

## 2026-09-09 22:37Z — dev routine run
- **Reviewed + QA'd + merged PR ugurozsahin/sky-academy-private-archive#149** (`Closes #40`, speech `cancel()`+`speak()`) → `4c61823`. Reviewer ≠ developer (PR opened by the 20:37Z run). Re-derived rather than inherited: the six speech tests proven **non-vacuous** by restoring the old body while keeping the injectable engine — all six fail; the guard rail proven both ways; rail count checked by running `main`'s own `guardrails.test.ts` (**34 → 35**), so the merge resolution the author flagged twice dropped none. QA on `7478202`: tsc clean, unit **471/471**, build 152.42 kB, **full mobile+desktop e2e 78/78 in 16.0 min**. Answered both reviewer questions on the PR: queueing (not silencing) was the author's to choose, since silencing would remove a feature a pre-reader depends on; the queued-clears-a-deferred-line window is unreachable in play (a slice arrives on a pointer event, a separate task from the deferral's macrotask). Recorded but not filed as an issue: `screenScope().dispose()` cancels speech but does not know about `say()`'s module-level `deferred` timer, so a line deferred in the turn before a route change can outlive the teardown — the only same-turn say-then-navigate site (`home.ts:75`) *wants* that line, so the change improves it.
- **Developed #30** (`.qcard` backdrop blur over the live arena) → PR ugurozsahin/sky-academy-private-archive#150, branch `claude/bold-knuth-dm2c0o`. `.play .qcard` drops `backdrop-filter`; the `.memory` head and the overlay/certificate rules keep theirs. Guard rail in the **e2e spec** (Vitest cannot read CSS text without passing vacuously), flagging any element whose backdrop filter overlaps the arena canvas — proven both ways. tsc clean, unit 471/471, build 152.42 kB (JS byte-identical, CSS +0.06 kB). No budget moved, no dependency.
- **Deviated from #30's own prescription, with evidence.** The issue asks for an opaque-ish card background as well; `arena.topInset` is the card's bottom edge, so nothing is ever drawn behind it. A/B'd on one page load with the arena frozen: dropping the filter alone moves **0.2 %** of the card's pixels (mean 0.4/255), dropping it *and* taking the alpha to .93 moves **84 %** (mean 3.34/255). So the look does not change and the PR is **not** `owner-approval` — I said on the issue at the start that it would be, and corrected it there. Frame rate re-measured under 6× throttling, ABBA-ordered: **43.0 → 46.7 fps (+8.6 %)**, against the issue's +11.8 % on another machine.
- Checks: nightly on main green (08:08:46Z, `6d0332c`, within 26 h) · watchdog pulse #61 at 17:05Z, ~5.5 h old · freeze holding, #46 order untouched · no `watchdog`-labelled defect open (only #74, `owner-input`).
- **Correction, added 03:30Z after the 00:38Z run reviewed it:** the "deviated with evidence" line above is wrong, and ugurozsahin/sky-academy-private-archive#150 no longer does that. `topInset` bounds bubble *apex* only, so `floatText` (every correct answer, `play.ts:125`) finishes ~20 px inside the card and the blur was hiding real ink. Worse, the A/B that convinced me otherwise froze a *quiet* arena — that removed the only thing which could differ, so it could not have failed. `3c3922d` takes the opaque-ish panel #30 asked for (alpha .93), the rail now pins the alpha floor as well as the missing filter, and the PR carries `owner-approval`. The reviewing agent was right.

## 2026-09-10 00:38Z — dev routine run
- **Reviewed PR ugurozsahin/sky-academy-private-archive#150** (`#30`, `.play .qcard` drops `backdrop-filter`) and **blocked it**: draft + `REVIEW: CHANGES REQUESTED`, `review-gate` now red on `c178156`. The diff is fine and the rail is a good one (proved it both ways myself — reverting the rule fails it with `qcard: blur(8px)`), but the PR's load-bearing claim is false. `topInset` constrains **bubble apex only** (`arena.ts:113`, `apexMin = topInset + r + 10`); nothing else on the canvas reads it. `floatText` (`play.ts:125` → `arena.ts:196`) spawns at `topInset + 40` with `vy -60` and `kind: 'text'`, which takes **zero gravity** in `update()` — it climbs 66 px and ends at `topInset − 26`, i.e. 20 px inside a card whose bottom edge is `topInset − 6`, on every correct answer. Burst particles rise up to ~210 px. Confirmed from the arena's own state after one real `answer()`: `behindCard: ["text@233"]`, `topInset 252`, card bottom `251`.
  - Measured over a real mission (20 s, mobile, canvas bitmap under the card vs a gradient-only baseline): the arena paints behind the card on **175/1201 frames — 14.6 %**, peak 0.85 % of the region, peak channel delta **172/255**. Against a panel that is `rgba(16,22,48,.78)`, that reads through.
  - Re-ran the author's own A/B (one page load, one paused canvas frame, toggle only the rule) but paused on a **busy** frame instead of a frozen arena: **1.00 %** of card pixels differ vs **0.20 %** on the quiet frame he measured. The pictures are the point — without the blur a legible `+10` and four sharp specks read through the card where main showed one soft smear. **The look does change in play**, so it is not mergeable as "no visual change"; the PR needs either #30's opaque panel or the `owner-approval` label.
  - Two comments that would have outlived the PR: `game.spec.ts:604` says "The card is opaque-ish in play now" (it is not — `--panel` untouched), and `style.css:520` quotes the issue's 49.0 → 54.8 fps as this change's own measurement (this PR measured 43.0 → 46.7).
- **Developed #41** (`noise()` builds a fresh random AudioBuffer per SFX) → PR below, branch `claude/bold-knuth-6wmii9`. One lazily built 0.5 s buffer per context, played as sub-ranges with `start(when, offset, duration)` and a random offset so a shared buffer does not make every swish bit-identical. The linear 1 → 0 fade the old buffer baked into its samples becomes a gain ramp — identical envelope, and what `tone()` has always done. 5 unit tests + 1 guard rail (`no SFX asks for more noise than the one shared buffer holds (#41)`), all proven both ways: reverting `noise()` fails 4 of the 5 and the rail.
- Tests: tsc clean · unit **477/477** (36 rails, was 35) · build 152.61 kB · **mobile e2e 39/39 (12.1 min)**. No budget raised, no dependency, no visual change (audio-only).
- Checks: nightly on main green (`6d0332c`, 2026-09-09T08:08:46Z, within 26 h) · watchdog pulse #61 at 23:05Z, ~1.5 h old · freeze holding, #46 order untouched · no `watchdog`-labelled defect open (only #74, `owner-input`).

## 2026-09-10 02:38Z — dev routine run
- **Reviewed + QA'd + merged PR ugurozsahin/sky-academy-private-archive#151** (`Closes #41`, one shared noise buffer) → `13fad42`. Reviewer ≠ developer (the 01:04Z run opened it). The PR asked a reviewer to push on the envelope — the old linear fade lived in the samples *before* the highpass, the new one is a gain ramp *after* the filter chain, and multiplication does not commute exactly with an LTI filter. Measured rather than accepted: all twelve real `noise(dur, gain, hp, lp)` calls through an RBJ biquad model of the Web Audio filters, both orderings, same noise, 20 draws, 5 ms RMS windows gated at −30 dB below each sound's own peak. Worst audible-range window **3.15 dB** — `earth` (`noise(0.12, 0.18, 80, 500)`), its final 5 ms, where the envelope is no longer slow relative to an 80–500 Hz band; every other sound ≤ 0.7 dB and no sound's total energy moves more than 0.1 dB. The claim holds, so the "sound must not change" criterion is the reviewer's to verify and not the owner's to approve.
  - Also instrumented `BaseAudioContext.prototype.createBuffer` / `AudioBufferSourceNode.prototype.start` in a real mobile-emulated Chromium, since the unit tests' stand-in context cannot say whether Chromium accepts `start(when, offset, duration)` on a shared buffer: **one** `createBuffer` (22050 samples @ 44.1 kHz — the device rate, not the test's 48 kHz), every `start()` with 3 arguments, four distinct offsets, nothing past the buffer end, nothing thrown.
  - Re-proved both ways: the old `noise()` body fails 4 of the 5 unit tests and the new rail. QA: tsc clean, unit 477/477 (36 rails), build 152.61 kB, **full mobile+desktop e2e 78/78 in 15.0 min**.
- **Developed #36** (session-callback object, the deferred half) → PR below, branch `claude/bold-knuth-97yzg4`, `Part of #36`. Three type-or-markup moves, no behaviour changed: the stage pill's **393-char** `innerHTML` (the longest line in play.ts and the only one over 180 outside a `render()` template) became the pure `stageHTML` builder in hud.ts, byte-identical over 640 generated cases; `onCorrect`/`onWrong`/`onMiss` collapsed onto one `settle()` driven by an `OUTCOME` table; and `'correct' | 'wrong' | 'miss'`, written out in three places, became `Outcome` in hud.ts.
- Tests: tsc clean · unit **484/484** (38 rails, was 36) · build 152.78 kB · mobile e2e green. Budget rail `play.ts long lines` lowered **5 → 4**; no budget raised, no dependency, no visual change.
- Checks: nightly on main green (`6d0332c`, 2026-09-09T08:08:46Z, within 26 h) · watchdog pulse #61 at 23:05Z, ~3.5 h old · freeze holding, #46 order untouched (only the stale "Eight left" count corrected to "Four left" to match its own checkboxes) · no `watchdog`-labelled defect open (only #74, `owner-input`).

## 2026-09-10 04:38Z — dev routine run
- **Reviewed + QA'd + merged PR ugurozsahin/sky-academy-private-archive#152** (`Part of #36`, the stage pill / the outcome beat / the `Outcome` union) → `aca0f29`. Reviewer ≠ developer (the 02:38Z run opened it). The PR named its ordering argument as the risk and asked to be pushed on it, so all three claims were re-proved rather than read:
  - **The reveal/floatText swap is unobservable.** `reveal()` writes `frozen`/`bubbles`/`nextId` and only *reads* `W`/`H`/`topInset`/`ctx`; `floatText()` does one thing, push to `particles`. Disjoint arrays, neither writes what the other reads, both inside one synchronous callback with no frame between them. Same for the DOM three (`#stage` / `#toast` / `#taunt`, distinct elements) and for `lastOutcome`, which is set before `settle()` can return down any path and is read only from `onWaveEnd`/`onEnd`. `showTaunt()` moving after `markSeg()` shifts no `Math.random()` draw either, since `markSeg`/`drawStage`/`stageHTML` consume none.
  - **`stageHTML` byte-identity re-proved wider than the PR did:** `main@13fad42`'s template held verbatim beside the new builder over **3,960 cases** (the PR ran 640) — 6 stage names incl. `<b>`, double quotes, an apostrophe, `<script>`, an accent and the empty string × 6 segment patterns × totals 0–10 × indices −2…total+2. All equal.
  - **Real-browser differential vs `main`:** a Year 1 mission on a 390×844 mobile context driven through all three `settle()` paths (correct → wrong → correct), snapshots taken on state conditions rather than fixed delays. `#stage` outerHTML, lit hearts, `#qcard` classes and score identical at every beat, twice, no console or page errors; the correct/wrong screenshots match, `+10` float text included. The look does not change, so this was the reviewer's to verify and not the owner's to approve — no `owner-approval` label, correctly.
  - Both new rails re-proved red on their own regressions. Budget confirmed by measurement: `play.ts` lines >180 chars 5 → 4, longest line 393 → 285, file 363 → 375 (the stated cost). QA: tsc clean, unit 484/484 (38 rails), build 152.78 kB, **full mobile+desktop e2e 78/78 in 20.3 min**.
- **Developed #43** (the deferred `layoutWave()` half) → PR below, branch `claude/bold-knuth-aj8nz1`, `Part of #43`. `spawnWave`'s inline layout — radius, air time, batching, each bubble's arc, colour and launch moment — becomes the pure exported `layoutWave(o, geom, speedK, now, rng)`; `spawnWave` keeps only what needs the 2D context (`fitLabel`) and the push. `bubbleRadius(W, H, wide)` exported alongside it, and `dealOrdered` now takes the `rng` too.
  - **The extraction found a real bug in itself.** A throwaway differential against `main`'s inline maths (1,440 layouts: 6 viewports × 3 speeds × wide/narrow × 1–10 labels × sequence/plain × 2 speed multipliers) disagreed on label order at n=2 — `dealOrdered` held a **second** `Math.random`, in the per-batch shuffle, that the first extraction had missed. Threaded; the differential then agreed exactly on all 1,440. Production behaviour is unchanged either way, since the only call site binds `rng` to `Math.random`.
  - 10 unit tests (determinism, labels dealt once, nothing clipped by an edge over 3 viewports × 1–10 labels × 30 seeds, apex below the question card and a rise of half the air time, batching fits the width, the 4-per-flight sequence cap on a tablet, the launch timetable, #32's multiplier touching only time, the crowding shrink) — three proved red on real regressions (dropping the ordered cap, dropping the margin, dropping `topInset` from the apex floor).
  - 1 guard rail (`the wave layout draws only from the rng it is given (#43)`), which is the rail for the bug above: both functions must take `rng: Rng`, neither body may contain `Math.random`, and `layoutWave` must keep its single production call site. Proved red both ways.
- Tests: tsc clean · unit **494/494** (39 rails, was 38) · build 153.03 kB (+0.25 kB, the extracted function) · mobile e2e green. No budget raised, no dependency, no visual change.
- Checks: nightly on main green (`6d0332c`, 2026-09-09T08:08:46Z, within 26 h) · watchdog pulse #61 at 2026-09-09T23:05Z, ~5.5 h old · freeze holding, #46 order untouched · no `watchdog`-labelled defect open (only #74, `owner-input`) · PR ugurozsahin/sky-academy-private-archive#150 left alone: draft + `REVIEW: CHANGES REQUESTED` from the 01:04Z run, and only that reviewer clears it.

## 2026-09-10 06:38Z — dev routine run
- **Reviewed + QA'd + merged PR ugurozsahin/sky-academy-private-archive#153** (`Part of #43`, `layoutWave()` extracted out of `spawnWave`) → `455ba62`. Reviewer ≠ developer (the 05:39Z run opened it). The PR asked its reviewer to re-derive the equivalence rather than trust its throwaway differential, so this run built one that does not rely on transcription at all: `main@aca0f29`'s `arena.ts` pulled with `git show`, its `spawnWave` body **mechanically** de-`this`-ified by script (which asserts no `this.` survives), `Math.random` left in place on that side and stubbed globally with a seeded mulberry32, main's own 3-arg `dealOrdered` imported alongside it — and the branch side handed the same seed while `Math.random` was replaced by a thrower.
  - **2,880 layouts / 15,840 bubbles, every field identical** (`r`, `waveT`, `batchSpan`; per bubble `label`, `x`, `vx`, `vy`, `g`, `launchAt`, `color`, `wobble`, and `y === H + r`). That is the PR's own grid **doubled by a `topInset` dimension its sweep never varied**. The branch side never touched `Math.random`.
  - Harness proved sensitive on two deliberate perturbations: swapping the `color`/`wobble` draw order (`expected '#ff5f6d' to be '#ffd54f'`) and dropping `topInset` from `apexMin` — the latter failing first at **seed 1001, the first `topInset: 120` case**, so the added dimension earned its place.
  - The two places the extraction could have gone wrong quietly both check out: `const k = gameSpeed()` was shadowed by the loop's `k` in main, so renaming it `speedK` leaves `PALETTE[(k * 3 + …)]` reading the loop index; and `fitLabel` moving out of the loop shifts no draw, since it consumes none.
  - QA: tsc clean, unit 494/494 (39 rails), build 153.03 kB, **full mobile+desktop e2e 78/78 in 15.6 min**, CI green, `review-gate` success. The look does not change (the differential *is* that proof), so this was the reviewer's to verify and not the owner's to approve — no `owner-approval` label, correctly. One non-blocking note left on the PR: `fitLabel`'s doc comment was split by the insertion point and its first line now sits above `bubbleRadius`, ~60 lines from the function it describes.
- **Developed #43** (the last deferred piece, `Tracer.result()`) → PR below, branch `claude/bold-knuth-7hksxa`, `Closes #43`. `Tracer.mark()` and `paint()`'s sampling loop become the pure exported `markPoint`/`paintStroke` over a plain `TraceGrid`/`TraceTally`, so the pass rule that decides whether a child's letter is accepted is testable with a synthetic glyph and synthetic strokes instead of a real canvas. `Tracer` keeps the drawing and holds the grid and tally.
  - Behaviour-identical to `main`, on the same kind of differential: main's `mark()` and `paint()` sampling extracted mechanically and run beside the new functions over **400 random masks × 6 strokes (2,400 strokes) plus 200 masks × 20 points** — every covered cell and every counter identical. Proved red on a changed disc radius (`1.2 → 1.3`) and a changed sample spacing (`/4 → /5`).
  - 7 unit tests, proved red on real regressions (dropping the claim-once guard: `expected 280 to be 64`; claiming the scan square instead of the brush disc: `expected [9, 18] to deeply equal [0, 0]`).
  - 1 guard rail (`the tracing pixel accounting stays canvas-free and unit-testable (#43)`), proved red both ways — inlining the accounting back into `Tracer.paint`, and un-exporting `markPoint`.
- Checks: nightly on main green (`6d0332c`, 2026-09-09T08:08:46Z, ~22 h at the start of this run, within 26 h) · watchdog pulse #61 at 05:05Z, ~1.5 h old · freeze holding, #46 order untouched · no `watchdog`-labelled defect open (only #74, `owner-input`) · ugurozsahin/sky-academy-private-archive#150 still blocked by the 01:04Z reviewer's `REVIEW: CHANGES REQUESTED` **and** an unanswered `owner-approval`, left alone.
- Still unreconciled, flagged by four runs now: **#79 and #83 carry the `review` label but are absent from #46's Code health list**, so the freeze arithmetic and the owner's list disagree. Owner's list to reconcile — no run should add to it.

## 2026-09-10 08:39Z — dev routine run
- **Reviewed + QA'd + merged PR ugurozsahin/sky-academy-private-archive#154** (`Closes #43`, the tracing pixel accounting) → `cf039d7`. Reviewer ≠ developer (the 07:12Z run opened it). The PR asked its reviewer to re-derive the equivalence rather than trust its own throwaway differential, so this run built one from `main`'s real code: `git show main:src/game/tracing.ts`, `mark()`'s block sliced out by index and rewritten **mechanically** (`private mark(x, y) {` → `function markMain(S, x, y) {`, then `this.brush`→`S.brush`, then `this.`→`S.`), with an assertion that no `this.` survives; `paint()`'s two sampling lines located by `String.includes` and joined with `this.mark(` → `markMain(S, `; both fed to `new Function`, so what ran was main's code and not a reading of it.
  - **500 masks × 6 strokes and 250 masks × 20 points**, seeded mulberry32 — random grid sizes, 1–4 overlapping letter blobs, brush 2–30, endpoints from −0.3…1.3 of the grid so strokes land on, off and half off the paper, plus a zero-length tap every sixth stroke. `covered`, `glyphHits`, `insidePts`, `outsidePts` compared after **every** stroke. All identical.
  - Harness proved sensitive on **five** perturbations, each caught on the first mask: disc `r*r*1.2`→`1.3`; spacing `/4`→`/5`; `tol = r*2`→`r*2+1`; `Math.round`→`Math.floor` on `cx`; dropping the claim-once guard (`expected [48,0,0] to deeply equal [14,0,0]`).
  - QA: tsc clean, unit 502/502 (40 rails), build 152.93 kB (−0.10 kB), **full mobile+desktop e2e 78/78 in 16.2 min**, CI green, `review-gate` success, not a draft. No `owner-approval` — the criterion here is that the look must *not* change, which is the reviewer's to verify. **#43 is closed**; `arena`, `visuals` and `tracing` all have unit cover now.
- **Developed #79** (`Part of #79`, not `Closes`) → PR below, branch `claude/bold-knuth-da2cro`. The issue's first criterion is "measure before guessing", and the measurement **overturns the issue's own hypothesis and #81's arithmetic**:
  - Instrumented mobile run of `main@cf039d7`: `goto` → first screen painted is **482 s of a 762 s suite (63 %)**, 13.03 s per test. The avatar walk is 0.90 s (4.4 %), map→island→topic 0.94 s (3.6 %), and **topic tap → first wave ready is 31 ms (0.1 %)**.
  - A probe pinned the 13 s to the render-blocking `fonts.googleapis.com` stylesheet in `index.html`: abort that one request and the same load is **0.33 s**. Not navigation, not the 11 avatar webps, not `waitUntil`.
  - **CI does not pay it** — from run `34448742260`'s log, `pause and quit return home` is **5.2 s** on CI against 15.4 s here. So it is a *local* cost: the suite is ~3× slower behind a network allowlist, which is why #32's "runnable locally" goal is still unmet, and every scheduled run and reviewer pays it.
  - **#81's premise does not survive the same log.** The spec is one file and Playwright parallelises by file, so the run uses both workers only because there are two projects: desktop file 5.7 m, whole run 9.0 m ⇒ mobile ≈ 9.0 m concurrent. Dropping desktop drops the *shorter* file and leaves a 9.0 m mobile file with a worker idle — **#81 saves ≈ 0 on the bill** until #72 adds a third project or the file itself is parallelised (`test.describe.configure({ mode: 'parallel' })`, worth ~50 %, but it puts the frame-rate rail under CPU contention and needs its own PR).
  - Shipped: `seedPlayer()` writes the save `avatarScreen` would have written into an init script (conditionally, so a test that reloads reads what *it* stored) — **951 ms → 99 ms** per test on the CI-like path, ~0.85 s each, 35 of 39 tests. The avatar-screen tests keep walking, and `first play shows the slice tutorial hand` still walks avatar → map → island → play so the cold start keeps one end-to-end test. 1 guard rail, proved red three ways (unconditional seed write; tidying the last cold start away; renaming `storage.ts`'s `KEY`). No test loses an assertion. `vx` compression and the unscaled `later(…)` waits are untouched and named as deferred.
  - CI e2e step before: **540 s (9 m 00 s)** on run `34448742260`; after is this PR's own run.
- Checks: nightly on main **green** (`455ba62`, 2026-09-10T08:08:22Z, ~30 min old) · watchdog pulse #61 at 05:05Z, ~3.5 h old · freeze holding, #46 order untouched (#43's line was auto-ticked by GitHub when the `Closes` merged) · no `watchdog`-labelled defect open (only #74, `owner-input`) · ugurozsahin/sky-academy-private-archive#150 still blocked by the 01:04Z reviewer's `REVIEW: CHANGES REQUESTED` **and** an unanswered `owner-approval`, left alone.
- #46's Code health prose still says "**Four left**" while listing five unticked items (#79, #81, #30, #36 + #74 `owner-input`); the owner added #79/#81 at 07:24Z above that line. His list, his count — noted, not edited.

## 2026-09-10 12:32Z — deadlock-breaking review run (not the dev routine)
- **Merged PR ugurozsahin/sky-academy-private-archive#150** (`Closes #30`, the question card's backdrop blur) → **`8e78693`**. This run neither opened nor previously reviewed it; it was invoked to break a process deadlock, and the deadlock was real.
- **What was stuck.** The 00:38Z dev-routine run set `REVIEW: CHANGES REQUESTED` on ugurozsahin/sky-academy-private-archive#150 at 00:46:46Z — correctly: the PR claimed "no visual change", but `topInset` bounds bubble *apex* only, so `floatText` finishes ~20 px inside the card and dropping the blur would have shown a sharp `+10` through it. The developer accepted it, pushed `3c3922d` (background to `rgba(16,22,48,.93)`, rail extended to pin the alpha floor), and the **owner approved at 06:55:19Z**. But CLAUDE.md:26 says only the reviewer who set a block clears it, and that reviewer's session never came back — its last activity anywhere in the repo was 00:53Z. The PR sat draft + red for ~11.5 h through at least three routine cycles. The watchdog filed **#86**.
- **The exception, stated as one.** This run cleared another reviewer's block. What licensed it was the conjunction of four things — the setter is gone; the owner has ruled on the only substantive question (this *does* change the look in play); the author correctly refused to clear his own block; and the clearer is neither the author nor a party to the original disagreement. Remove any one and the answer is no. **It is not a new norm**: it does not license clearing your own block, nor a live reviewer's. The escalation path (#86) is what should be used again, and it worked.
- **Verified before acting, not assumed.** The `OWNER: APPROVED` marker is the owner's own — a bare two-word comment with no `Generated by Claude Code` footer, matching the genuine precedent on ugurozsahin/sky-academy-private-archive#132 (12:08:12Z) and unlike the agent-written one on ugurozsahin/sky-academy-private-archive#132 at 11:41:07Z that was withdrawn under CLAUDE.md:4. No later `REVIEW: CLEARED` existed.
- **The review was not a rubber stamp, and it caught something the green tick could not have said.** ugurozsahin/sky-academy-private-archive#150's CI was green on `d8e8aa0` — but from 03:31Z, and four commits (ugurozsahin/sky-academy-private-archive#152/ugurozsahin/sky-academy-private-archive#153/ugurozsahin/sky-academy-private-archive#154/ugurozsahin/sky-academy-private-archive#155) had landed on main since, **ugurozsahin/sky-academy-private-archive#155 rewriting the very e2e helpers the new rail calls**. So the tick was not evidence about what would land. This run trial-merged onto `main@12f9a6e` in a scratch clone: merge clean, `tsc` clean, unit **503/503**, build **152.93 kB**, the #30 rail still listed on **mobile and desktop**, `pickAvatar`/`startTopic` intact, and `play.ts` still setting `topInset` from the qcard and still firing `floatText` — so the alpha floor is still load-bearing after the merge. A full CI run was then dispatched on merged `main` (a push to main only gets the light run, so e2e would otherwise have waited for the nightly).
- **The alpha claim was checked, not taken.** `globalAlpha = 1 − life/max`, so the first glyph pixel reaches the card at `k ≈ 0.63`; bleed-through is `(1−A)·k·Δ`, giving ~27/255 at `.78` (a legible `+10`) against ~9/255 at `.93`, decaying to ~4/255 — a ~3.1× cut, and dimmer overall than the blur it replaces, which spread that energy rather than removing it. `rgba(16,22,48,.93)` is exactly `--panel` with the alpha raised, and no `.qcard.good`/`.bad`/`.pulse` rule sets a background, so the outcome beat cannot override it. The rail's one blind spot (`.overlay`, `.cert-view` measure zero-size mid-play) was checked at the code level: `render()` early-returns while `paused` (#31/ugurozsahin/sky-academy-private-archive#145) and `play.ts` pauses the arena before every overlay, so nothing re-filters. Justified.
- The PR body still argued the *pre-review* position ("no visual change", "I did not do the opaque-ish half"), which `3c3922d` reversed. It was not allowed to become the commit record: the squash carries an explicit message describing what actually ships and naming the owner approval.
- **#30 closed** by the merge, all six checklist boxes ticked. **#86 closed** with the resolution.

## 2026-09-10 ~14:1xZ (independent reviewer session — a different agent from the ugurozsahin/sky-academy-private-archive#164 developer)

- **PR ugurozsahin/sky-academy-private-archive#164 merged to `main` (`1359a89`, squash) — #36 closed.** The `Session({...})` callback object moves out of `playScreen()` into a new **`src/ui/play-session.ts`** (200 lines), taking with it the state only it touches: the outcome beat (`lastOutcome` / `revealUntil` / `waveId`, `endWave`, `settle`, the `OUTCOME` table), the `#53` mission progress pill, the `#44` font gate, `prevLives`, `promptHTML` and `BOMB`. `arena.onWaveEnd` went too — it reads the outcome still on screen. `play.ts` 375 → 269 lines, the last slice of the split that ran ugurozsahin/sky-academy-private-archive#99 → ugurozsahin/sky-academy-private-archive#117 → ugurozsahin/sky-academy-private-archive#121 → ugurozsahin/sky-academy-private-archive#152.
- **Behaviour preservation was checked, not accepted.** A normalised token diff of the moved bodies against `main` leaves only the deps indirection, two extracted locals and `OUTCOME` naming its hold by key. The `window.__sna` hooks block is byte-identical to `main`. On the teardown path, `cleanup()` destroys the arena rather than nulling it and `arena` is assigned exactly once, so the new lazy `arena: () => arena` resolves to the same object the old closure variable did; `play-session.ts` adds no raw timer or listener, and `play.ts` keeps no copy of any of the moved state.
- **Guard rails: two added, three repointed, none weakened.** `play.ts` long lines stay at **4**; `play-session.ts` starts at **0**; new rail "the Session callbacks live in play-session.ts". The reviewer re-ran all five regressions rather than taking the PR's word: reverting the split, a 200-char line in the new file, a second `markSeg()` in `onMiss`, dropping the `#44` font gate and unqueueing the `#40` progress speech each go red, and 44 rails green with the change in.
- **Tests:** `tsc` clean · unit **506/506** (24 files, 44 rails) · build **153.42 kB JS / 38.36 kB CSS**, CSS byte-identical to `main` (md5 `5af7907a…`), JS +0.49 kB · `playwright --list` 80 tests · PR CI green including the mobile e2e. `main` had not moved since the PR's green run, so the branch tip *was* the merge base — no rebase needed.
- **#36's remaining deferral was retired rather than rolled forward.** Its Done box still listed `home.ts` / `style.css` long lines; `style.css` has since reached its floor of 0, and `home.ts`'s remaining 8 are `render()` HTML template literals — the same irreducible floor as `play.ts`'s 4. So `Closes #36` is correct, and the checklist says why.
- **Note for whoever shares the owner's Mac checkout:** a second session was working in `~/mnt/education-game` on `chore/159-stale-green` while this review ran. All of this review's git work was done in a throwaway clone; that worktree was left untouched.

- **PR ugurozsahin/sky-academy-private-archive#163 (#79) reviewed and BLOCKED — draft + `REVIEW: CHANGES REQUESTED`, `review-gate` now red.** The change passed review on its own merits: on base `405db84`, `tsc` clean, unit **507/507**, and all four perturbations it claims reproduce (drop `* speedK` → 3 failures; restore `later(hideTutorial, 9000)`; drop the rAF check; drop the `myWave` capture). The `waveId` guard is the right fix in `endWave`'s own idiom, and "no behaviour change at speed 1" is an identity, not a claim — `scaled(ms)` is `ms / 1` and `factor` is pinned at 1 unless `?fast=N` / `__SNA_FAST` / `setSpeed()`.
- **It was blocked because ugurozsahin/sky-academy-private-archive#164 landed first and moved the code out from under it.** `onQuestion`, its `spawn` closure and the rAF inside it, `settle()`'s tracing advance and `onEnd`'s results floor are all in `play-session.ts` now. A trial merge onto `main` in a scratch clone gives `CONFLICT (content): Merge conflict in src/ui/play.ts`, and the branch's two new rails are anchored on text that has left `play.ts` — the queued-spawn rail fails with *expected -1 to be greater than -1*, and the real-time-beat rail, scoped to `play.ts` alone, would go green while the file the beats moved to is unrailed. That last one is the dangerous half: a rail that narrows silently, not one that fails loudly.
- **This is exactly the stale-green case #88 is about.** `b6a18c1`'s tick is about base `405db84`; `main` is two commits past it, and one of those two rewrote the file. Both PRs reported green against the same base; only the first one to merge was entitled to it. Worth repeating whenever two PRs touch one file: the second one's tick expires the moment the first lands.
- The rebase instructions are on PR ugurozsahin/sky-academy-private-archive#163 and summarised on #79. #79 stays open with **Review** ticked and **QA** not — the e2e must be re-run against the rebased tree, mobile *and* desktop (since #81 a PR runs mobile only, and desktop is where the superseded-wave bug showed). Acceptance criterion 4's measurement (540 s → 406 s, −25 %) stands and needs no redoing.

## 2026-09-10 ~14:2xZ–14:3xZ (independent reviewer session — author of neither PR)

- **Both remaining review PRs merged. ugurozsahin/sky-academy-private-archive#166 first (`fa82ccd`, closing #83), then ugurozsahin/sky-academy-private-archive#165 (`83b8214`, closing #88).** Final `main`: `tsc` clean, unit **527/527** (24 files), build **153.42 kB / 53.27 kB gzip**, `playwright --list` 80 tests.
- **ugurozsahin/sky-academy-private-archive#166 — the closing-keyword rule.** `CLAUDE.md` / `BACKLOG.md` / `docs/ROUTINE-PROMPT.md` carry the rule next to the existing `Part of #<n>` sentence; `closingRefs(body)` lands in `scripts/review-gate.mjs` with a guarded, import-safe CLI; `tests/unit/review-gate.test.ts` pins it.
- **Its central claim was re-measured, not accepted.** The PR said the helper agrees with GitHub's own `closingIssuesReferences` on all 73 PRs. Re-run here over every pull request via GraphQL: **75/75** (73 plus ugurozsahin/sky-academy-private-archive#165 and ugurozsahin/sky-academy-private-archive#166). Confirmed on the real bodies that GitHub closed #26 from ugurozsahin/sky-academy-private-archive#83/ugurozsahin/sky-academy-private-archive#86/ugurozsahin/sky-academy-private-archive#90, #35 from ugurozsahin/sky-academy-private-archive#120 and #44 from ugurozsahin/sky-academy-private-archive#139 — every one of them merely *discussing* the close.
- **ugurozsahin/sky-academy-private-archive#166's rails perturbed rather than trusted.** Naive `closingRefs` → **7** red (the PR claimed 6, understating), with every corrected-wording case still green; the rule deleted from `CLAUDE.md` alone → the three-file rail red on that file only; a live issue number injected into the docs' bad example → that rail red. Restored: 48/48.
- **ugurozsahin/sky-academy-private-archive#165 — `ci.yml` now subscribes to `ready_for_review`.** Parsed with PyYAML to exactly `[opened, synchronize, reopened, ready_for_review]`: all three defaults kept, so it tightens and loosens nothing.
- **ugurozsahin/sky-academy-private-archive#165's rail perturbed past what the PR claimed.** Its four cases reproduce (drop `ready_for_review` → red; delete the whole `types:` line → red; drop `synchronize` → red; block-sequence reformat → green, no false red). Two more were added here: moving the correct `types:` under `push:` → red, so the narrowing to the `pull_request` sub-block is real; and — the one that mattered — a comment *literally* spelling the four events above a line missing one → still red. **The PR's own test A proved less than it said**: `ci.yml`'s trigger note never contains the literal token `ready_for_review`, so removing it from the line removed it from the file entirely. Re-run properly, the rail is genuinely comment-blind (ugurozsahin/sky-academy-private-archive#129).
- **ugurozsahin/sky-academy-private-archive#165's three-file argument judged, not counted, and it holds.** `BACKLOG.md` carries the freeze wording (whose "change all three together" is scoped to that wording) and the scrum line; it says nothing about CI, drafts or merge evidence. `docs/WATCHDOG-PROMPT.md` already states the principle for `main` at line 51. Editing `BACKLOG.md` would have been churn.
- **Staleness, both PRs.** ugurozsahin/sky-academy-private-archive#166's branch point was 3 commits behind `main`, including ugurozsahin/sky-academy-private-archive#164's `play-session.ts` refactor — trial-merged in a private worktree before merging: clean, 526/526. ugurozsahin/sky-academy-private-archive#165 was then trial-merged onto `main` *with ugurozsahin/sky-academy-private-archive#166 in it*.
- **The two-branch conflict was real, and ugurozsahin/sky-academy-private-archive#166's advice about it was wrong.** ugurozsahin/sky-academy-private-archive#166's body said the `CLAUDE.md` collision would be "the text is identical, so take either side". It is not: the two PRs edit **adjacent bullets**, so git conflicts the pair as a single hunk with a different genuine change on each side — ours the review-block bullet (#88), theirs the routine bullet (#83). Taking either side wholesale would have silently dropped one of the two rules. Resolved by keeping **both**. `docs/ROUTINE-PROMPT.md` auto-merged (ugurozsahin/sky-academy-private-archive#166 adds a paragraph in STEP 3; ugurozsahin/sky-academy-private-archive#165 edits STEP 2 and merge rule 1). Verified afterwards that each rule appears exactly **once** on `main`.
- **The new rule was obeyed on the way in.** The rebase was pushed to ugurozsahin/sky-academy-private-archive#165 as `41afd1a`, which fired a `synchronize` run; the merge waited for **that** run (success) rather than the older tick on `9c53ab0`. That run is also live evidence the restated `types:` list still covers `synchronize`.
- **Confirmed `9c53ab0` did its job**: the paragraph belonging to #83 is absent from ugurozsahin/sky-academy-private-archive#165's tree and present exactly once per file in ugurozsahin/sky-academy-private-archive#166's.
- **Freeze status: still on.** #83 and #88 are closed with every checklist box ticked, no `debt` issue is open, but **#79 remains open** (`review`, P1) — PR ugurozsahin/sky-academy-private-archive#163 is still draft + blocked pending its rebase onto ugurozsahin/sky-academy-private-archive#164, per the 14:2xZ entry above. Feature work does not restart yet.
- **Isolation:** all git work done in a throwaway `git worktree` with a private temp dir, removed afterwards. `~/mnt/education-game` was not touched.

## 2026-09-10 ~14:5xZ (independent reviewer session — clearing the block this same session set on ugurozsahin/sky-academy-private-archive#163)

- **PR ugurozsahin/sky-academy-private-archive#163 merged to `main` (`566a356`, squash) — #79 closed.** `vx` now carries `speedK`, so fast mode is a pure time compression (same launch x, same apex, same landing x, fewer seconds); the beats still in real time go through `scaled()`; and the superseded-wave bug it uncovered is fixed with the `waveId` idiom `endWave` already uses, applied at **both** deferrals.
- **The block from 14:2xZ was cleared by the agent that set it, after re-checking rather than reading the tick.** Right twice over: `main` had moved again since the rebase (`fa82ccd` #83, `83b8214` #88, `42c1ac3`) and GitHub reported the branch `dirty`. The staleness caught on this PR the first time applied to its second green tick as well.
- **The review's fourth point was settled by experiment rather than argument, which is the part worth keeping.** The block had asked for the "no beat left in real time" rail to count `play-session.ts` as well as `play.ts`. Narrowed back to `play.ts` alone with a real-time `deps.later(() => {}, 1200)` live in `play-session.ts`, the suite goes **fully green (73/73)** — the narrowing is invisible. A rail scoped to a file the code has moved out of is not a weakened rail anyone can see; it is one that reports success. Next time a split moves code between files, repointing the rails is part of the split, not follow-up.
- **Perturbations re-run by the reviewer on the merged tree**, not taken from the PR: drop `* speedK` → RED ×3; bare `later(hideTutorial, 9000)` in `play.ts` → RED; bare `later(fn, 1200)` in `play-session.ts` → RED (the widening working); drop the rAF check → RED; drop the `myWave` capture → RED. No budget moved, no rail deleted.
- **Desktop was bought deliberately.** Since #81 a pull request runs mobile only, and this bug only ever showed on desktop. Merging on the mobile tick would have left the one project that has ever caught this defect unrun on the final tree, so the full matrix was dispatched by hand against that exact tree (`workflow_dispatch` run `34490771784`) and came back green on **both** projects. ~4 minutes. The `ready_for_review` run #88 had just enabled fired too, so the merge rested on two fresh runs and no old ones.
- **Reviewer integration merge, stated rather than buried:** `tests/unit/guardrails.test.ts` conflicted because #79 and #83/#88 each appended rails at the tail, leaving the branch's last `it(...)` unclosed. Resolved as the strict union in `0d39c34` — 0 deletions against either parent, +44 lines from `main`, +56 from the branch, and no source file or e2e test touched — rather than spending a third block-and-rebase cycle on a tail conflict.
- **Tests on `0d39c34`, the tree that landed:** tsc clean · unit **530/530** (24 files) · build **153.48 kB JS / 38.36 kB CSS**, CSS md5 unchanged from `main` · `playwright --list` 82 tests · PR CI green · full matrix green on mobile and desktop.
- **Footnote that #83 paid for within the hour:** this PR's only `Closes` sits inside backticks, which `scripts/review-gate.mjs` had just measured as closing nothing — it names ugurozsahin/sky-academy-private-archive#163 in that very list. A plain `Closes #79` went into the squash message on purpose, and the issue closed on merge. ugurozsahin/sky-academy-private-archive#166 landing a few hours earlier turned that from a surprise into a decision.

## 2026-09-10 ~16:1xZ–16:2xZ (independent reviewer session — author of neither the PR nor the freeze)

- **PR ugurozsahin/sky-academy-private-archive#169 merged to `main` (`7ebb86d`, squash): the code-health freeze is over.** Owner-approved at 15:34Z, which is the whole gate on a loosening; `review-gate` green on the head and stamped 15:34:11Z, after his comment, so it read it. The merge carries no closing keyword and shut nothing — checked with `scripts/review-gate.mjs` beforehand and against the issue list afterwards.
- **The premise was re-queried rather than taken from the body.** `state=open&labels=review` → 0, `labels=debt` → 0. Had either been non-empty the freeze's own condition would still have held and the lift would have been overriding it, not satisfying it.
- **The three files agree byte-for-byte.** The 1,004 characters shared by `CLAUDE.md`, `BACKLOG.md` and `docs/ROUTINE-PROMPT.md` hash identically; each then adds its own tail. It replaces the rule instead of deleting it — #46's order plus the priority labels, `playtest` and red `main` still first, STEP 3 reading #46 top-down, STEP 4 "do not invent work #46 does not list", and "re-impose a freeze the owner has lifted" added to the Do NOT list. The incidental "the freeze says code health comes first" in merge rule 4 was caught too. A tree-wide grep for freeze/frozen/🧊 leaves only game code, the e2e helper, the stood-down artifact and history.
- **The sentence that matters survives a hostile read**, verbatim in all three: *"The lift is a one-time event, not a condition that can re-arm."* The freeze outlived its purpose because it was written as a condition that every new `review`-labelled process finding re-armed; without that sentence the next one re-freezes the repo by reading.
- **The new rail was perturbed, not trusted.** Reworded CLAUDE.md's one-time sentence back into a condition → red; dropped the lift line from `BACKLOG.md` alone → red; restored the old rule in the present tense in `docs/ROUTINE-PROMPT.md` alone → red. Restored: 6/6. Both halves of its claim hold. Its `toMatch(/#46/)` assertion is near-vacuous, though — all three files name #46 elsewhere — and should be anchored to the surrounding sentence next time that file is touched.
- **Issue #46 was diffed against its own pre-PR revision** (11:08:42Z, via the GraphQL edit history), not eyeballed: 35 items before, 35 after, identical order, none added or dropped. Beyond what the PR body described, five boxes were ticked (#86, #79, #81, #30, #36) and "Four left" became "All clear" — every one of the five verified genuinely shut between 12:37Z and 14:50Z, so it is bookkeeping the earlier merges skipped. #74 correctly stays unticked: `owner-input`/`watchdog`, never `review`/`debt`. An edit to the owner's list should still say everything it touched.
- **`frozen` is off #8, #11, #14, #15** and no issue anywhere carries it; the label itself still exists, the deletion left to the owner.
- **No staleness this time, and it was checked rather than assumed.** `origin/main` was `5351dff`, exactly this PR's merge base, so the 15:01Z tick was about the tree that landed. Re-run anyway on `df78a88` in a private worktree: tsc clean · unit **536/536** (24 files) · build **153.48 kB** · `playwright --list` 82 tests. PR ugurozsahin/sky-academy-private-archive#167 is still open and also appends to `tests/unit/guardrails.test.ts`; it wants a rebase now — a tail conflict, not a semantic one.
- **On what replaces the freeze's pressure:** the PR's soft-ratchet proposal (above N open `review`/`debt` issues, the next run takes one before starting a feature) is worth doing, and it is a tightening, so it does not need the owner's gate. The queue emptying in four days is evidence about a period when the freeze was doing the pushing; what remains afterwards is code health competing on priority, and 6 September's failure was not mis-ranking but nobody looking. A threshold on a count cannot silently re-arm the way "any open issue" did.
- **Isolation:** all git work in a throwaway `git worktree` with a private temp dir, removed afterwards. `~/mnt/education-game` was not touched.

## 2026-09-10 ~19:0xZ — dev routine (cloud)

**Merged two PRs, developed the #58 plan.** First run in three with a WORKLOG entry — see the note at the foot.

**Health (STEP 1).** Nightly green: `event=schedule` [`34453560118`](https://github.com/ugurozsahin/sky-academy/actions/runs/34453560118), 08:08Z on `455ba62`, ~11 h old, inside 26 h. Watchdog pulse #61 stamped 17:08Z, ~2 h. `main` green at `2350c37`. No open `watchdog` defect (#74 is the owner's spend decision, `owner-input`).

**Reviewed and merged (STEP 2)** — I opened neither, so both were mine to judge.

- **ugurozsahin/sky-academy-private-archive#172 → `96f393a`** (`Part of #57`, save export/import). Verified rather than read off the PR: tsc clean, unit **542/542**, build **155.56 kB**, full mobile + desktop e2e **84/84 in 17.0 min** run here. Merge base checked before merging — `main@2350c37` landed 17:00:55Z and the PR's run started 17:05:20Z, so the tick already covered the tree that landed (the ugurozsahin/sky-academy-private-archive#150 mistake). `style.css` long-line budget 0, tree 0; nothing raised. The look is the author's to verify, not the owner's to approve, and I re-verified it because they could not attach images: driven at 390×844 and 1280×800, `scrollWidth === clientWidth` at both, nothing in `.p-move` past the viewport, no page errors.
- **ugurozsahin/sky-academy-private-archive#173 → `38893a1`** (`.android/` + signing-key ignores). Docs + `.gitignore`, both path-filtered, so no CI run is expected — not a red light. Claims verified against the code: `android.yml:37-40` really does decode `ANDROID_DEBUG_KEYSTORE` to `~/.android/debug.keystore` behind `if: env.KS != ''`; no tracked file matches the new patterns. It closes a real hole — Capacitor's generated `android/.gitignore` ships `*.keystore`/`*.jks` **commented out**, the stock template. Tightening, so routine-mergeable.

**Found on ugurozsahin/sky-academy-private-archive#172, filed not blocked — #95 (P3).** `importSave()` gates on the version only, so `{"v":1}` is accepted and wipes the save, and `{"v":1,"progress":null}` is accepted, **persisted**, and then throws in `parentSummary` *and* on the map (`home.ts:53` — `?.` guards the value, not the null container), leaving no in-app way back. Not blocked: it needs a hand-crafted blob, `load()` has had the same exposure since it was written, and the PR is the only thing standing between an APK update and a wiped save. The version gate does hold against every realistic stray paste — prose, URLs, truncated codes, smart quotes, `[{"v":1}]`, `{"save":{"v":1}}` all refused; whitespace-wrapped codes correctly accepted; no `__proto__` pollution.

**Developed (STEP 3) — the #58 plan**, which the owner asked for on 2026-09-10 ("propose the plan on this issue before writing code… do not implement anything"). No code written. The finding: **4 topics of 76 are genuinely unplayable without a voice, not the curriculum** — all 54 maths topics already print what they speak. And the mechanism bug under it: `Question.listen` is gated on the read-aloud *toggle* (`hud.ts:52`, `play-session.ts:118`), not on whether the device can speak, so on the Android WebView this issue is about, the fallback we already built is invisible exactly when it is needed. Recommended text-only over bundled audio with the numbers (~300 clips, 3–7 MB, does not scale to #11). Three decisions put to the owner.

**Recurring friction, now three runs.** #46 still carries nothing filed since #86: #94, #90, #87, #72 and #8 are all `P1` + `routine-ok` and absent from the owner's list, so STEP 3's "first unchecked item in #46" walks past them into Features. Not mine to reorder — flagged again. WORKLOG itself: this session has a designated push branch and may not push to `main`, so this entry goes to `claude/bold-knuth-b5n4v4` (no CI — `ci.yml` push is `branches: [main]`). Two prior runs skipped the entry for this reason; worth the owner settling whether the routine's branch may commit this one file to `main`, or whether STEP 5(b) should drop WORKLOG and let the heartbeat be the record.
